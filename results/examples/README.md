# Sanitized examples

M0's redacted marker-only result is stored under `m0-npm12/`. The current example is an Inconclusive infrastructure record because Docker was unavailable; it is not npm lifecycle evidence. Later milestone examples require their own human review.
