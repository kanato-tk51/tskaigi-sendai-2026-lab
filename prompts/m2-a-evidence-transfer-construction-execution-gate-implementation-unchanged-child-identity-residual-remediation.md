# Goal

Remediate only frozen-research issue #43's remaining M2A-CGI04 unchanged-
child-identity finding from the fresh private-authority residual-remediation
re-review. Make every permitted held-directory transition preserve the exact
identity of every unchanged child, authorize only the operation-specific
created, removed, renamed, replaced, or nested-mutation child, and expose the
same decision through the separately branded fake trace. Do not reopen the
closed process-settlement or parent-path findings, acquire npm or toolchain
bytes, execute a production entry, construct a context or image, call Docker,
run a lifecycle or transfer, or access runtime/result state.

# Read first

- root `AGENTS.md`, `packages/AGENTS.md`, and `experiments/AGENTS.md`
- `docs/index.md` and the issue #43 documents routed there
- `docs/frozen-research-execution-plan.md`
- `docs/presentation-scope.md`, especially the resumed/deferred high-assurance
  boundary
- `docs/milestones.md` M0, M2-A, and frozen-research sections
- `docs/codex-workflow.md`
- `docs/threat-model.md`
- `docs/experiment-protocol.md`
- `docs/architecture.md`
- `docs/experiment-matrix.md`
- `docs/spike-npm12.md`
- `docs/m2-a-npm-lifecycle-adapter.md`
- `docs/reviews/m2-a-npm-lifecycle-adapter.md`
- `docs/m2-a-evidence-transfer-contract.md`
- every issue #43 transfer implementation/remediation review routed by
  `docs/index.md`
- `docs/m2-a-evidence-transfer-construction-execution-gate.md`
- all construction/execution-gate contract review and remediation records
- all construction/execution-gate implementation prompts, paired review
  prompts, and review records routed by `docs/index.md`
- `prompts/m2-a-evidence-transfer-construction-execution-gate-implementation-private-authority-residual-remediation.md`
- `prompts/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-private-authority-residual-remediation-review.md`
- `docs/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-private-authority-residual-remediation.md`
- this prompt and its paired fresh re-review prompt

# Scope

Implementation and verification changes are limited to this strict subset of
the unchanged M2A-CG06 allowlist:

```text
experiments/npm12-install/scripts/m2a-transfer-production.mjs
experiments/npm12-install/scripts/m2a-transfer-production.d.mts
experiments/npm12-install/scripts/verify-m2a-transfer-static.mjs
tests/m2a-evidence-transfer.test.ts
```

Update only these existing status paths as needed to record remediation
evidence and the fresh re-review handoff:

```text
experiments/npm12-install/README.md
docs/m2-a-evidence-transfer-construction-execution-gate.md
docs/m2-a-evidence-transfer-contract.md
docs/m2-a-npm-lifecycle-adapter.md
docs/index.md
docs/milestones.md
docs/frozen-research-execution-plan.md
```

This prompt and its paired fresh re-review prompt were saved before any
unchanged-child-identity source or test repair and must remain unchanged during
implementation. Preserve every unrelated accumulated worktree change.

# Out of scope

- M2A-CGI01 through M2A-CGI03, M2A-CG01 through M2A-CG03, first-cause
  compiler/Docker settlement, and the held-parent/path replacement boundary
  are closed and immutable for this task.
- Do not redesign public attempt, completion, image-binding, construction-
  manifest, transfer, adapter-event, or evidence schemas; the fake-only held-
  directory declaration may change only enough to carry bounded child identity
  data used by the production transition verifier.
- Do not change construction source/declarations, package scripts, lockfiles,
  Containerfile, transfer manifest, initializer/runner sources, adapter/probe
  sources, package manifests, fixtures, scenarios, historical/results,
  Expected, or `Observed` bytes.
- Do not approve or perform acquisition, production compilation/construction,
  image build/inspect, Docker/runtime, lifecycle, transfer, result review,
  cleanup, retry, ingestion, publication, deployment, or evidence promotion.

# Constraints

## Preserved closed boundaries

1. Preserve the exact `20260721-01` tuple, 31-row and 41-row aggregates,
   M2A-CGR01 through M2A-CGR03 contract closure, M2A-TR01 through M2A-TR06
   earlier Docker-free static/unit closure, M2A-CGI01 through M2A-CGI03, and
   implementation-scope M2A-CG01 through M2A-CG03.
2. Preserve the shared first-cause/first-exit compiler and Docker settlement
   state machine, all fixed deadlines/output bounds/argv/environment, the
   canonical image/runtime transactions, chronological first issue, write-
   ahead checkpoint, conditional-copy order, and combined candidate.
3. Preserve exact held parent descriptor/path correlation, full mode and
   special-bit absence, effective owner, link-count and BigInt parent identity,
   same-descriptor attempt write/reread/close, held-directory sync, copied-file
   metadata validation, fail-closed ownership settlement, and the explicit
   cooperative-host limitation.
4. Keep production constructors, authorities, and brands private and distinct
   from fake brands. They remain absent from public declarations and package
   scripts and reachable only behind the unchanged no-argument fail-closed
   entry gates. Reusable imports remain side-effect-free.

## Exact child identity and operation-specific transition

5. Use one child-identity validator in both the production held-directory
   transition and the separately branded fake trace. Each snapshot must bind
   every exact lexical entry name and type one-to-one to its full child
   identity: type, full mode, effective owner/group, link count, and BigInt
   device, inode, size, and mtime. Missing, extra, duplicate, reordered,
   accessor/inherited, symlink, special, sparse, or type-disconnected child
   identity data must reject before transition adoption.
6. Every child name present before and after an operation is unchanged unless
   the fixed production call site explicitly names that one child as the
   operation-specific mutation. An unchanged child's complete identity must be
   equal before and after; a same-name device/inode, link, mode, owner, size,
   or mtime replacement must reject and must never become the next held
   baseline.
7. Represent the fixed production deltas exactly, without a caller-extensible
   mutation list: exclusive creation of `attempt.next`; rename of that exact
   inode to `attempt.json`, including replacement of the prior canonical
   attempt when present; creation of the current fixed Docker-copy destination;
   creation of `probe-output/`; and creation of its fixed marker. A rename must
   carry the exact source child identity to the exact destination name. An
   addition must be absent before and present exactly once after. A removal or
   replacement must be the fixed counterpart of that exact operation. No
   unrelated child may change.
8. Reject cross-name identity aliases. Except for the explicit source-to-
   destination identity carried by the fixed rename, no two names may share a
   device/inode pair, no newly created or replacement destination may alias an
   unchanged sibling, and the contract link count must remain exact. A
   hardlink alias, destination substitution, or same-name sibling replacement
   must reject even when entry names/types and the parent identity are exact.
9. Handle the conditional marker as one correlated nested mutation. The held
   marker parent may add only `direct-write-marker.json`; the already held
   transfer parent may update only the existing `probe-output` child's allowed
   directory size/mtime caused by that same operation while preserving its
   type, full mode, owner/group, link count, device, and inode. All other
   transfer and marker-parent children remain byte-for-byte identity-equal.
   Do not broadly refresh or adopt the transfer child map.
10. Validate the full before snapshot, exact operation delta, and full after
    snapshot before assigning any parent or child identity as the next held
    baseline. On rejection, retain the prior baseline and stop before the next
    child, copy, checkpoint, marker, validation, publication, or final result.
    Remove every path by which the complete post-operation child map is adopted
    after only parent and entry-name/type checks.

## Behavioral fake and negative coverage

11. Extend only the separately branded fake held-directory trace so it can
    express the same full child identities and fixed operation kinds consumed
    by production. It must accept no host path, descriptor, production brand,
    production constructor, callback, executable, argv, cwd, environment, or
    process authority. Fake snapshots are canonical plain data and cannot
    substitute for production authority.
12. Preserve the existing positive add/rename, copy, marker, and parent-drift
    coverage and add behavioral positives for exact unchanged siblings,
    attempt replacement carrying the `attempt.next` identity, top-level copy,
    and the nested marker-parent/transfer-parent update.
13. Add negatives for an unchanged same-name child's device/inode, mode,
    owner/group, link, size, and mtime drift; a created destination hardlinked
    to an unchanged sibling; two after names sharing one identity; a renamed
    destination not carrying the source identity; a replacement of an
    unrelated prior attempt/copy child; unauthorized `probe-output` object
    replacement during marker creation; and fake child data that is missing,
    extra, reordered, aliased, or disconnected from entries. Every fake action
    log must end at the affected `before:<index>` barrier.
14. Static checks must prove the production transition and fake trace call the
    same child-identity decision boundary, every production mutation call site
    supplies one fixed operation-specific delta, production child maps are not
    blindly adopted, fake-only declarations do not expose production
    authority, and the four-path implementation allowlist is preserved.
    Marker-name assertions or a parallel test-only reducer do not close the
    finding.

## Safety and evidence boundary

15. All future acquisition/toolchain/context/image digests and the local image
    ID remain `null`; build/runtime approvals remain `false`; every
    `evidenceReview` remains `not-performed`; and current entry calls still
    fail before authority creation or filesystem/process activity. Do not call
    an entry to prove this.
16. Tests use only in-memory separately branded fake traces and repository-
    owned disposable test roots already allowed by the reviewed contract. They
    must not import or execute a production entry or either container source,
    open a fixed acquisition/construction/runtime/result root, spawn a compiler
    or Docker process, signal a real child, or inspect retained state.
17. Passing checks remain Docker-free static/unit and cooperative-host
    evidence. They do not establish npm/toolchain acquisition, filesystem
    publication, Docker behavior, image identity, lifecycle behavior, named-
    volume transfer, result validity, runtime enforcement, or `Observed`.

# Deliverables

- The smallest source/declaration repair inside the four-path allowlist that
  closes the remaining M2A-CGI04 unchanged-child-identity contradiction.
- Behavioral fake-only child-identity and operation-delta tests that exercise
  the same production transition decision rather than parallel marker logic.
- Static reachability/import/allowlist checks preserving the closed production
  authority and evidence boundaries.
- Minimal status updates recording observed Docker-free verification and the
  fresh independent re-review handoff without promoting evidence.

# Verification

Run only the approved Docker-free checks:

```sh
npm run m2a:transfer:verify
npm run m2a:verify
npm run typecheck
npm test
npm run check
git diff --check
```

Also run focused Prettier checking over the exact four-path implementation
allowlist, this saved prompt pair, and changed status files. Do not format or
repair unrelated dirty files. Record an aggregate failure without editing an
out-of-scope path, and do not claim `npm run check` passed unless every stage
completes.

Do not run `m0:doctor`, `m0:build`, `m0:run`, `m0:verify`, a production entry,
Docker, npm acquisition/install/pack/approve/rebuild, a compiler, a lifecycle,
or any command that touches fixed ignored runtime state.

# Completion report

- Closure evidence for the remaining M2A-CGI04 unchanged-child-identity
  finding and resulting M2A-CG04 through M2A-CG06 implementation status while
  preserving M2A-CGI01 through M2A-CGI03 and M2A-CG01 through M2A-CG03
- Exact unchanged-sibling, rename/replacement, copy, nested marker-parent,
  hardlink-alias, and pre-adoption stop traces
- Changed files and commands actually run with observed results
- Intentionally unrun commands, preserved unrelated work, remaining
  cooperative-host/runtime limitations, and evidence class
- One concrete `Next:` task naming the fresh independent Docker-free re-review
  under
  `prompts/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-unchanged-child-identity-residual-remediation-review.md`
