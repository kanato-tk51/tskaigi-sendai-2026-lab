# Goal

Perform a fresh independent Docker-free read-only implementation/gate review of
the issue #45 distinct activation-object wrapper/run-ID/package implementation.
Decide whether the implementation exactly realizes the approved M4-AG01 through
M4-AG06 contract and whether at most one later argument-free candidate command
occurrence may be approved. Do not execute or approve the command unless every
review finding closes in this separate review record.

# Read first

- root `AGENTS.md` and `containers/AGENTS.md`
- `docs/index.md` and `docs/frozen-research-execution-plan.md`
- the issue #45 documents selected by `docs/index.md`
- `docs/m4-distinct-activation-object-execution-gate.md`
- `docs/reviews/m4-distinct-activation-object-execution-gate.md`
- `prompts/m4-distinct-activation-object-execution-gate-remediation.md`
- `docs/reviews/m4-distinct-activation-object-execution-gate-remediation.md`
- `prompts/m4-distinct-activation-object-execution-gate-implementation.md`
- this prompt

# Required independent checks

1. Reproduce the fixed `20260720-02` Expected/run/root/container tuple,
   unchanged profile/input/image bytes, constants compiler delta, activation
   construction/manifests/sets, exact 32-source/64-output inventories, wrapper
   source/JavaScript/declaration bytes, and package/profile-README delta.
2. Derive the exact ordered seven built-in source and emitted-JavaScript
   bindings, zero repository imports, singleton wrapper construction/executable
   closures, package-only production inbound edge, path-only activation child,
   and rejection of every missing/extra/reordered/alternate/unleased loading
   form.
3. Trace every held-object capture/validation/release relation, exact new-root
   absence checks, pairwise non-aliasing, two pre-spawn/two post-`close`
   validations, same-descriptor reads, synchronous no-child four-validation
   branch, returned-child/late-close ownership, and identity/release output
   suppression without probing a result root.
4. Trace fixed spawn arguments, empty environment, one-child ceiling,
   timeout/output bounds, TERM/KILL delivery failures, exit/close agreement,
   unknown settlement, and the chronological write-once failure latch. Prove
   later valid complete/Inconclusive bytes cannot displace any earlier failure.
5. Reproduce strict child/outer canonical schemas, exact step prefixes,
   private-field suppression, control/exit correlation, and no output before
   settlement, both post-use checks, and successful reverse release.
6. Run the exact Docker-free compile/static/unit verification and compiled
   import-safety assertion without importing/executing the wrapper or
   activation entry. Decide every focused negative case and ordinary
   fail-closed/non-reachability assertion.
7. If and only if all findings close, record an exact one-occurrence execution
   gate bound to the reviewed hashes, argument-free script, preflight/root
   absence conditions, output/retention boundary, and later mandatory result
   review. Otherwise record one bounded remediation and do not approve the
   command.

# Scope

Read-only repository inspection, repository-owned in-memory compiler/AST/schema
assertions, the approved Docker-free verification commands, a new independent
review record, and minimal status/handoff updates after the decision.

# Out of scope

- Importing or executing the wrapper or activation object, running the
  candidate command or another M4 production command, Docker/container/socket
  access, retained-state or result-root access, cleanup, repair, retry, or
  signaling.
- Implementation edits, new identities/generation, issue #46 work, historical
  evidence or `Observed` change, external network, credentials, host-home or
  non-repository executable inspection, Remote Git, publication, or deployment.

# Verification

Use only the contract-approved Docker-free compile/static/unit commands,
repository-owned inputs, focused formatting, and `git diff --check`. Do not
run either entry, the candidate command, Docker, a production executor, or any
result-root/path-existence probe.

# Completion report

Report explicit finding decisions, exact reproduced values, commands actually
run, commands intentionally not run, evidence classes, residual limitations,
and exactly one `Next:` item. Distinguish implementation approval from command
approval and do not claim a separate human review.
