# Goal

Perform a fresh independent Docker-free read-only re-review of only frozen-
research issue #43's M2A-NGR01/M2A-NGR02 npm-acquisition execution-gate
remediation. Decide whether the exact host command and process-observable
entry authority now agree without a false lexical-argv claim, whether the
acquisition-root directory-link rule now matches production while preserving
one-link files, and whether M2A-NG01/M2A-NG03 therefore close at contract
scope. Do not repair the contract/source/tests, import or execute the
producer, access fixed or host runtime state, or use external communication.

# Read first

- root `AGENTS.md`, `packages/AGENTS.md`, and `experiments/AGENTS.md`
- `docs/index.md` and the complete issue #43 chain routed there
- `docs/frozen-research-execution-plan.md`
- `docs/presentation-scope.md`, especially the resumed/deferred
  high-assurance boundary
- `docs/milestones.md` M0, M2-A, and frozen-research sections
- `docs/codex-workflow.md`
- `docs/threat-model.md`
- `docs/experiment-protocol.md`
- `docs/architecture.md`
- `docs/experiment-matrix.md`
- `docs/spike-npm12.md`
- `docs/m2-a-npm-lifecycle-adapter.md`
- `docs/reviews/m2-a-npm-lifecycle-adapter.md`
- `docs/m2-a-evidence-transfer-contract.md`
- the complete issue #43 transfer, construction/execution-gate, and
  dependency-input contract/implementation/remediation/review chain routed by
  `docs/index.md`
- `docs/m2-a-evidence-transfer-npm-acquisition-execution-gate.md`
- `prompts/reviews/m2-a-evidence-transfer-npm-acquisition-execution-gate-review.md`
- `docs/reviews/m2-a-evidence-transfer-npm-acquisition-execution-gate.md`
- `prompts/m2-a-evidence-transfer-npm-acquisition-execution-gate-remediation.md`
- the exact remediation implementation/contract/status allowlists
- this prompt

# Scope

Review only the M2A-NGR01/M2A-NGR02 remediation diff, the remediated
execution-gate contract, the saved prompt pair, and tracked source needed to
reproduce the three-file identity, import/call graph, entry guard, directory
creation, file publication, static verifier, and focused tests. Preserve every
unrelated accumulated worktree change and every immutable earlier review.

The only permitted repository writes are:

```text
docs/reviews/m2-a-evidence-transfer-npm-acquisition-execution-gate-remediation.md
experiments/npm12-install/README.md
docs/m2-a-evidence-transfer-npm-acquisition-execution-gate.md
docs/m2-a-evidence-transfer-dependency-input-boundary.md
docs/m2-a-evidence-transfer-construction-execution-gate.md
docs/m2-a-evidence-transfer-contract.md
docs/m2-a-npm-lifecycle-adapter.md
docs/index.md
docs/milestones.md
docs/frozen-research-execution-plan.md
```

# Out of scope

- Repairing implementation, contract, static verifier, tests, either saved
  remediation prompt, package scripts, lockfiles, manifests,
  Containerfiles/container source, adapters/probes/packages, fixtures,
  scenarios, historical/results, Expected, or `Observed`
- Importing or executing either producer; reading, listing, statting, or
  creating fixed acquisition/toolchain/construction/result roots; reading the
  host Node executable, environment, process report, installed packages,
  home/cache, credentials, proxy/certificate configuration, or retained state
- DNS, external/loopback/Unix-socket communication, npm acquisition,
  install/pack/approve/rebuild, lifecycle execution, child processes,
  compiler/constructor execution, image build, Docker/runtime socket,
  transfer, candidate/result validation, cleanup, repair, retry, signaling,
  Remote Git, publication, deployment, or third-party communication
- Approving or performing the external occurrence, substituting standing
  authorization, accepting npm bytes, changing reviewed construction
  bindings, or promoting evidence
- Reopening M2A-NG02, M2A-NG04, M2A-NG05, or M2A-NG06 without a newly
  reproduced contradiction

# Constraints

## M2A-NGR01 decision

1. Recompute the exact size and SHA-256 of the three ordered executable
   closure paths and their aggregate. Confirm the only expected executable
   change is the acquisition entry and reproduce the same two local import
   edges and closed acquisition call graph.
2. Reproduce the exact host command object: `/usr/bin/env`, fixed four-token
   argv after it, repository-root cwd, empty environment, and `shell: false`.
   Confirm the displayed serialized command corresponds exactly and no shell,
   interpolation, caller `PATH`, environment, stdin, URL, version, root, or
   retry becomes authority.
3. Trace the acquisition entry before its first producer call. Require exact
   `process.execPath`, original `process.argv0`, canonical `process.argv[0]`,
   argv length, canonical argv one equal to `fileURLToPath(import.meta.url)`,
   repository cwd derived only from the fixed module location, and zero
   environment own keys. Confirm every mismatch reaches
   `M2A_INPUT_AUTHORITY_REJECTED` before producer reachability.
4. Confirm the self-entry condition remains import-safe and that no new
   import-time filesystem, environment, process, request, constructor, Docker,
   transfer, result, or evidence side effect exists.
5. Confirm the remediated contract no longer claims the entry can observe the
   original lexical script spelling after Node canonicalization. It must bind
   alternate lexical spelling exclusion to the exact host command/preflight,
   while the entry independently binds only exact process-observable state.
   Reject a contract that silently treats those two boundaries as the same.
6. Reproduce the static verifier's entry-guard extraction and each
   in-memory weakening case. Confirm removing or substituting any executable,
   argv, cwd, empty-environment, guard-order, or pre-producer relation makes
   static verification fail. Disconnected `includes()` markers or test titles
   alone are insufficient.
7. Assess the residual cooperative-host limits: unbound executable/loader/
   trust-anchor bytes, canonicalization before entry observation, and
   same-authority/kernel mutation. A positive decision must not convert them
   into hostile-host or original-command-line proof.

## M2A-NGR02 decision

8. Inspect the exact `createHeldDirectoryChild()` boundary and confirm it
   accepts only a positive created-directory link count while retaining type,
   mode, owner/group, no-follow, parent identity, unchanged siblings, sync,
   and close constraints.
9. Confirm every acquisition-root directory claim and later candidate-review
   rule uses the same positive-link-count predicate and does not claim
   `links === 1` or another filesystem-independent exact directory value.
10. Confirm archive/receipt staging and final regular files still require
    exactly one link and that no broad source replacement weakened those
    checks.
11. Reproduce the function-scoped static inverse cases: zero directory links
    fail, positive values remain admissible, substituting exact-one for the
    directory predicate fails the static contract, and removing exact-one
    file checks also fails. Do not accept a source-wide occurrence count that
    cannot distinguish directory and file transitions.
12. Preserve the cooperative filesystem limitation. A positive link count is
    the exact admitted predicate, not authenticity, hostile-kernel, or
    machine-crash evidence.

## Preserved decisions and next boundary

13. Reproduce enough of M2A-NG02/M2A-NG04/M2A-NG05/M2A-NG06 to confirm the
    remediation did not change the credential-empty two-request transport,
    deadlines/bounds, root-first retained no-retry direction, exact process
    terminals, separate candidate review, or explicit-human external
    authority.
14. Confirm both reviewed construction bindings remain `null`,
    `runtimeExecutionApproved` remains `false`, every `evidenceReview` remains
    `not-performed`, and contract/static evidence remains distinct from an
    external occurrence, candidate, accepted input, construction, runtime
    result, and `Observed`.
15. A positive decision closes only M2A-NGR01/M2A-NGR02 and
    M2A-NG01/M2A-NG03 at contract/static entry-guard scope, completing
    M2A-NG01 through M2A-NG06 at contract scope. It does not authorize the
    external occurrence. The next safe task is to save the exact adjacent
    preflight/one-occurrence execution prompt and its separate Docker-free
    fixed-root result-review prompt before requesting explicit human
    external-communication authority.
16. A blocked decision assigns only the smallest remaining exact
    M2A-NGR01/M2A-NGR02 contradiction and permits no repair or occurrence.

# Deliverables

- `docs/reviews/m2-a-evidence-transfer-npm-acquisition-execution-gate-remediation.md`
  with an `APPROVED` or `BLOCKED` decision; M2A-NGR01/M2A-NGR02 and
  M2A-NG01 through M2A-NG06 status; reproduced source identity/import graph,
  entry/host authority, directory/file link predicates, static inverse
  coverage, preserved transport/result/authority analysis, commands,
  limitations, and one exact next boundary
- Minimal authoritative status updates only after the decision
- One concrete `Next:` item

# Verification

Run only:

```sh
npm run m2a:transfer:verify
npm run m2a:verify
npm run typecheck
npm test
npm run check
git diff --check
git status --short
```

Also recompute the exact three source identities and aggregate, and run
focused Prettier checking over the remediation allowlists, saved prompt pair,
new review record, and changed status files. Record aggregate failures exactly
without repairing unrelated files.

Do not run either producer, `m0:doctor`, `m0:build`, `m0:run`, `m0:verify`,
npm acquisition/install/pack/approve/rebuild, a lifecycle fixture, compiler,
constructor, image build, Docker, transfer, fixed-root inspection, or result
validation.

# Completion report

- Decision and M2A-NGR01/M2A-NGR02 / M2A-NG01 through M2A-NG06 status
- Reproduced host/process authority and lexical/canonical limitation
- Reproduced positive directory-link and exact-one file-link boundaries
- Reproduced static inverse coverage and fresh source identity/aggregate
- Preserved transport, occurrence, no-retry, candidate/result,
  construction-binding, external-authority, and evidence separation
- Changed files and commands actually run with observed results
- Intentionally unrun commands, preserved unrelated work, remaining
  cooperative-host/external limitations, and evidence class
- Confirmation that no producer, fixed root, host environment/runtime,
  external communication, npm candidate, construction, Docker, runtime/result,
  or `Observed` evidence was accessed or produced
- If approved, the exact safe prompt-only handoff before explicit human
  external-communication authorization; if blocked, only the smallest
  remediation
- One concrete `Next:` task
