# Goal

Perform a fresh independent Docker-free read-only re-review of only frozen-
research issue #43's M2A-CGI04 private-authority residual remediation. Decide
whether first-cause compiler/Docker settlement and held-directory/path
transaction correlation close M2A-CGI04 and M2A-CG02 through M2A-CG06 at the
Docker-free static/unit implementation boundary while preserving closed
M2A-CGI01 through M2A-CGI03 and M2A-CG01. Do not repair source or tests,
acquire npm or toolchain bytes, execute a production entry, construct a
production context or image, call Docker, run a lifecycle or transfer, or
access runtime/result state.

# Read first

- root `AGENTS.md`, `packages/AGENTS.md`, and `experiments/AGENTS.md`
- `docs/index.md` and the issue #43 documents routed there
- `docs/frozen-research-execution-plan.md`
- `docs/presentation-scope.md`, especially the resumed/deferred high-assurance
  boundary
- `docs/milestones.md` M0, M2-A, and frozen-research sections
- `docs/codex-workflow.md`
- `docs/threat-model.md`
- `docs/experiment-protocol.md`
- `docs/architecture.md`
- `docs/experiment-matrix.md`
- `docs/spike-npm12.md`
- `docs/m2-a-npm-lifecycle-adapter.md`
- `docs/reviews/m2-a-npm-lifecycle-adapter.md`
- `docs/m2-a-evidence-transfer-contract.md`
- every issue #43 transfer implementation/remediation review routed by
  `docs/index.md`
- `docs/m2-a-evidence-transfer-construction-execution-gate.md`
- all construction/execution-gate contract review and remediation records
- `prompts/m2-a-evidence-transfer-construction-execution-gate-implementation.md`
- both earlier construction/execution-gate implementation remediation prompt
  pairs and review records
- `docs/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-residual-remediation.md`
- `prompts/m2-a-evidence-transfer-construction-execution-gate-implementation-private-authority-residual-remediation.md`
- every changed implementation, declaration, verification, test, and status
  path in that prompt's exact allowlist
- this prompt

# Scope

Review only the M2A-CGI04 private-authority residual-remediation diff and its
minimal status records. Treat implementation summaries, reported command
passes, source marker names, and positive fake traces as claims to reproduce.
Preserve every unrelated accumulated worktree change and immutable earlier
review record.

The only permitted repository writes are:

```text
docs/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-private-authority-residual-remediation.md
experiments/npm12-install/README.md
docs/m2-a-evidence-transfer-construction-execution-gate.md
docs/m2-a-evidence-transfer-contract.md
docs/m2-a-npm-lifecycle-adapter.md
docs/index.md
docs/milestones.md
docs/frozen-research-execution-plan.md
```

# Out of scope

- Do not edit implementation, declaration, verification, test, prompt,
  package, lockfile, Containerfile, manifest, container source, adapter/probe,
  fixture, scenario, historical/result, Expected, or `Observed` paths.
- Do not reopen M2A-CGI01 through M2A-CGI03 or redesign the reviewed contract,
  schemas, fixed tuple, source aggregates, command order, or evidence classes.
- Do not approve or perform acquisition, production construction, image build,
  Docker/runtime, lifecycle, transfer, result validation, cleanup, retry,
  ingestion, publication, deployment, or evidence promotion.

# Constraints

## M2A-CGI04 process-settlement decision

1. Trace both real helper call sites to the same private first-cause state
   machine exercised by the separately branded fake-only driver. Confirm the
   fake surface accepts no executable, argv, cwd, environment, path, process
   handle, production brand, or production constructor.
2. Independently submit synchronous spawn failure; error then
   `close(0, null)`; nonzero exit then `close(0, null)`; zero exit then
   inconsistent close; timeout/overflow/signal failure followed by successful
   late events; close without exit; exit without close; output/descriptor
   uncertainty; duplicate/late events; exact success; and no-event final-bound
   traces. Reproduce the first failure, first exit tuple, exact timer ownership,
   bounded settlement, and no-later-phase action for every branch.
3. Confirm a terminal can succeed only with no failure cause, one zero/null
   first exit, an identical later close, and known child/stdout/stderr/
   descriptor closure. A later event must never improve or erase a prior
   failure or conflicting exit. Confirm all fixed deadlines, output bounds,
   executable/argv/cwd/environment, signal order, and no-retry/no-cleanup
   properties remain unchanged.
4. Reject marker-only tests or a fake reducer that is not the one consumed by
   both production process helpers. Do not spawn a real compiler or Docker
   child, signal a process, wait on real long timers, or execute an entry.

## M2A-CGI04 held-directory/path decision

5. Trace every production inventory, checkpoint/final attempt publication,
   copy destination, validation, marker-parent, rename, and directory-sync
   operation to the shared held-directory identity/transition verifier.
   Confirm initial and pre/post checks bind exact type, full mode/special-bit
   absence, owner, contract link count, and BigInt device/inode/size/mtime to
   the already held descriptor and exact no-follow pathname.
6. Reproduce the original stable held inode `1` / replacement path inode `2`
   contradiction with inode `2` carrying the exact expected inventory. It
   must reject before path-based inventory, write, copy validation, marker
   creation, or publication can advance. Exercise ancestor, owner, mode,
   special-bit, link, size/mtime, device/inode, and pre/post-operation drift.
7. Exercise exact-inventory contradictions for extra, missing, reordered,
   aliased, symlink, and special children at result, transfer, and conditional
   marker-parent phases. Exercise transient `attempt.next`, rename/sync
   mismatch, copy destination replacement, marker-parent replacement, and
   unexpected metadata transition. Confirm every action log ends at the exact
   affected barrier.
8. Confirm broad inventory-mutation adoption is absent. Every permitted
   size/mtime/link/inventory update must be operation-specific and adopted only
   after the held descriptor and pathname still name the same directory and
   the exact expected before/after state is proven.
9. Confirm attempt bytes are written/synced/reread through one no-follow file
   descriptor, its close settles before rename, pathname correlation brackets
   rename, and the already held result descriptor performs directory sync.
   Confirm copied files use exact absent destinations and same-descriptor byte/
   metadata validation after known child close.
10. Reject marker-only filesystem assertions or a fake verifier not consumed
    by the production transaction. Preserve the explicit cooperative-host
    limitation: pre/post pathname correlation reduces but does not eliminate
    same-authority path-swap or mount races and is not an OS filesystem sandbox.

## Preserved boundary and evidence decision

11. Reproduce the exact 31-row and 41-row aggregates and bind the changed-path
    identities. Confirm M2A-CGI01 through M2A-CGI03 positives and negatives,
    M2A-CGR01 through M2A-CGR03 contract closure, M2A-TR01 through M2A-TR06
    earlier static/unit closure, fixed tuple, schemas, and M2A-CG06 allowlist
    remain unchanged.
12. Confirm production constructors/brands remain private and absent from
    declarations, exports, package scripts, and tests; fake brands cannot
    substitute for production; imports are side-effect-free; and current entry
    calls still fail before authority creation without importing or executing
    an entry as proof.
13. Confirm future digests/image ID remain `null`, approvals remain `false`,
    every `evidenceReview` remains `not-performed`, and no fixed ignored root,
    context, image, Docker object, transfer, result, historical evidence,
    Expected, or `Observed` byte changed.

## Review method and decision ceiling

14. Use only repository source, in-memory calculations, separately branded
    fake process/filesystem traces, and repository-owned disposable test roots.
    Never use a fixed acquisition, construction, build, runtime, or result
    root. Do not import either container source.
15. The maximum positive decision closes M2A-CGI04 and M2A-CG02 through
    M2A-CG06 only at the Docker-free static/unit cooperative-host
    implementation boundary. It may permit only the smallest separately
    reviewed acquisition/toolchain input boundary already ordered by the
    contract. It does not acquire bytes, approve external communication,
    construct a context/image, choose an image ID, call Docker, approve a
    runtime occurrence, access a result, ingest M3, or promote `Observed`.
16. A blocked decision assigns only the smallest remaining M2A-CGI04
    contradiction and keeps every later issue #43 gate frozen. Do not repair a
    finding during this review.

# Deliverables

- `docs/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-private-authority-residual-remediation.md`
  with an `APPROVED` or `BLOCKED` decision, exact process and held-directory
  traces, M2A-CGI01 through M2A-CGI04 and M2A-CG01 through M2A-CG06 status,
  changed-path identities, commands run, evidence class, limitations, and one
  permitted next boundary
- Minimal authoritative status updates only after the decision
- One concrete `Next:` item

# Verification

Run only the Docker-free checks needed to reproduce the remediation claims:

```sh
npm run m2a:transfer:verify
npm run m2a:verify
npm run typecheck
npm test
npm run check
git diff --check
```

Also run focused Prettier checking over the exact remediation allowlist, the
saved prompt pair, the new review record, and changed status files. Record an
aggregate failure without editing unrelated files. Never claim a check passed
unless it actually exited successfully.

Do not run `m0:doctor`, `m0:build`, `m0:run`, `m0:verify`, a production entry,
Docker, npm acquisition/install/pack/approve/rebuild, a compiler, a lifecycle,
or any command that touches fixed ignored runtime state.

# Completion report

- Re-review decision and M2A-CGI01 through M2A-CGI04/M2A-CG01 through
  M2A-CG06 status
- Independently reproduced first-cause process and held-directory/path/
  inventory/publication/copy traces
- Preserved allowlist/import/evidence-class boundaries
- Changed files and commands actually run with observed results
- Intentionally unrun commands, remaining cooperative-host/runtime
  limitations, and preserved unrelated work
- If approved, the smallest separately reviewed acquisition/toolchain boundary
  without claiming external-network, construction, Docker, runtime, or result
  authority; if blocked, the exact smallest M2A-CGI04 remediation
- One concrete `Next:` task
