# Goal

Perform a fresh independent Docker-free read-only re-review of the bounded
M4-AGI01/M4-AGI02 remediation. Decide whether the external pre-command trust
anchor and the expanded focused negative matrix close both findings and fully
realize M4-AG02 through M4-AG05 at implementation scope. Do not run or approve
the candidate command unless every finding closes in this separate review.

# Read first

- root `AGENTS.md` and `containers/AGENTS.md`
- `docs/index.md` and `docs/frozen-research-execution-plan.md`
- the active issue #45 documents selected by `docs/index.md`
- `docs/m4-distinct-activation-object-execution-gate.md`
- `docs/reviews/m4-distinct-activation-object-execution-gate.md`
- `prompts/m4-distinct-activation-object-execution-gate-remediation.md`
- `docs/reviews/m4-distinct-activation-object-execution-gate-remediation.md`
- `prompts/m4-distinct-activation-object-execution-gate-implementation.md`
- `docs/reviews/m4-distinct-activation-object-execution-gate-implementation.md`
- `prompts/m4-distinct-activation-object-execution-gate-implementation-remediation.md`
- this prompt

# Required independent checks

1. Reproduce the exact reviewed wrapper source, JavaScript, and declaration as
   42,865 / 41,159 / 1,244 bytes with SHA-256 values `80829982...`,
   `ab36b509...`, and `ed1e6145...`. Confirm that a same-prefix body change and
   every appended loading form fail the full-object trust check before command
   eligibility.
2. Confirm the trust split is explicit and non-circular: the final fresh-worker
   `npm run m4:static` occurrence authenticates the three full objects, exact
   source/emitted bindings and edges, singleton closures, package-only inbound
   edge, parent inventories, package/profile bytes, and fixed generation; the
   production wrapper's descriptor capture proves only stability after that
   trust input. Confirm the two commands are adjacent with no intervening
   compiler, formatter, test, write, or repository mutation, and retain the
   cooperative-host race limitation.
3. Independently derive the exact seven ordered source and emitted built-in
   bindings, zero repository edges, singleton construction/executable closures,
   package-only production inbound edge, 32/64 inventories, and rejection of
   every missing, extra, reordered, alternate, absolute, relative, `file:`,
   package/subpath/import-map, dynamic/computed, `require`, `createRequire`,
   property/evaluation, activation/ordinary/backend/runner, emitted-binding, or
   otherwise unleased edge.
4. Reproduce the complete no-child and returned-child process matrix: separate
   third/fourth validation failure, reverse-release suppression, null streams,
   invalid PID, missing/duplicate/contradictory exit-close tuples, asynchronous
   error with and without later `close`, TERM/KILL delivery failure, and exact
   retention until `close`.
5. Reproduce late valid complete and Inconclusive child bytes after every
   earlier timeout/output/process/signal failure and confirm chronological
   first-failure precedence, null `controlResult`, exact one/two/four/five step
   boundaries, both post-use validations, reverse release before output, exact
   outer/activation key order, and private-field suppression.
6. Reproduce the exact two-root absence source instrumentation without probing
   either result root. Run the Docker-free typecheck, static verifier, focused
   wrapper tests, full M4 verification, compiled ordinary-module import-safety
   assertion without either frozen-research entry, formatting checks, and
   `git diff --check`.
7. If and only if M4-AGI01/M4-AGI02 and M4-AG02 through M4-AG05 close, record an
   exact at-most-one command gate. It must require a fresh worker to reproduce
   all reviewed identities and exact new-root absence, run `npm run m4:static`
   as the final filesystem-reading trust preflight, perform no intervening
   command or write, then use the standing authorization for exactly one
   argument-free `npm run --silent m4:execute:frozen-research` occurrence. Any
   failure exhausts the generation and authorizes no retry. Otherwise record
   one bounded remediation and do not approve the command.

# Scope

Read-only repository inspection, repository-owned in-memory compiler/AST/schema
assertions, approved Docker-free verification, one new independent review
record, and minimal issue #45 status/handoff updates after the decision.

# Out of scope

- Importing or executing the wrapper or activation entry, running the candidate
  command or another M4 production command, Docker/container/runtime-socket
  access, result-root or retained-state access, cleanup, repair, retry, or
  signaling.
- Implementation edits, new identities/generation, issue #46 work, historical
  evidence or `Observed` change, external network, credentials, host-home or
  non-repository executable inspection, Remote Git, publication, deployment,
  or external communication.

# Completion report

Report explicit M4-AGI01/M4-AGI02 and M4-AG02 through M4-AG05 decisions, the
trust-anchor decision, exact reproduced identities and negative matrix,
commands actually run and intentionally not run, evidence classes, residual
limitations, and exactly one `Next:` item. Distinguish implementation approval
from command approval and do not claim a separate human review.
