# Goal

Perform a fresh independent Docker-free read-only implementation review of the
frozen-research issue #43 M0/M2-A evidence-transfer static/unit implementation.
Decide M2A-TR01 through M2A-TR06 at implementation scope and whether exactly
one later construction/execution-gate task may be defined. Do not repair the
implementation, build an image, invoke Docker or npm lifecycle commands, bind
or approve a local image candidate, or execute a transfer.

# Read first

- root `AGENTS.md`, `packages/AGENTS.md`, and `experiments/AGENTS.md`
- `docs/index.md` and the issue #43 documents routed there
- `docs/frozen-research-execution-plan.md`
- `docs/m2-a-evidence-transfer-contract.md`
- `docs/reviews/m2-a-evidence-transfer-contract.md`
- `prompts/m2-a-evidence-transfer-implementation.md`
- this prompt

# Required independent checks

1. Reproduce the exact `20260721-01` tuple, historical non-reuse boundary,
   31-file ordered aggregate and anchors, fixed M2-A event/target/attempt
   contract, and absence of source or evidence promotion drift.
2. Trace every fixed command/inspection/copy/canonical-record validator and
   reject arbitrary image, command, argv, environment, mount, path, container,
   volume, fallback, retry, cleanup, and public production reachability.
3. Inspect without importing or executing the fixed initializer and runner.
   Reproduce their private-input, descriptor/identity, fixed npm child,
   loopback, settlement, completion publication, and no-late-fallible-operation
   boundaries.
4. Trace the fake-backend state machine across successful, failed, unknown,
   timeout, early-transfer, copy-validation, and first-failure-displacement
   paths. Confirm natural exit is required and all Docker objects are retained.
5. Reproduce canonical completion/attempt bytes and strict completion,
   producer-segment, marker, filesystem metadata, source/copy digest, and
   evidence-class validators, including the complete M2A-TR06 negative matrix.
6. Run only approved Docker-free static/unit verification and import-safety
   source checks. Do not import either container source, invoke a production
   entry, inspect a result root, or access Docker/retained state.
7. If and only if every finding closes, approve one separately defined
   Docker-free construction/execution-gate task that must bind all constructed
   bytes and the exact local image ID before any candidate command review.
   Otherwise record one bounded remediation and keep all later gates closed.

# Scope

Read-only repository inspection, repository-owned in-memory parser/schema
assertions, approved Docker-free verification, one new independent review
record, and minimal status/handoff updates after the decision.

# Out of scope

- Implementation edits, image construction, Docker/container/socket access,
  npm lifecycle execution, transfer, result-root/retained-state access,
  cleanup, retry, repair, evidence ingestion, result acceptance, or
  Expected/Observed change.
- Credentials, host-home/environment inspection, external network, Remote Git,
  publication, deployment, execution approval, or standing-authorization use.

# Verification

Use only contract-approved Docker-free focused/static/unit commands, focused
formatting, and `git diff --check`. Report exact observed results; do not claim
runtime isolation or transport success from static/unit evidence.

# Completion report

Record the decision and M2A-TR01 through M2A-TR06 statuses, exact reproduced
identities and state-machine boundaries, commands actually run, commands
intentionally not run, evidence classes, residual limitations, and exactly one
concrete `Next:` item.
