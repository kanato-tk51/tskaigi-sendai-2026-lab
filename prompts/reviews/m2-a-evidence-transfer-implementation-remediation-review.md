# Goal

Perform a fresh independent Docker-free read-only re-review of only frozen-
research issue #43's bounded M2A-TRI01 through M2A-TRI04 implementation
remediation. Decide whether the complete fixed host command/environment plan,
truthful initializer/publication descriptor settlement, exact complete npm
terminal, and closed canonical attempt state machine close M2A-TR03 through
M2A-TR06 while M2A-TR01/M2A-TR02 and every previously accepted boundary remain
intact. Do not repair implementation or tests, construct an image, define or
execute a runtime command, or accept evidence.

# Read first

- root `AGENTS.md`, `packages/AGENTS.md`, and `experiments/AGENTS.md`
- `docs/index.md` and the issue #43 documents routed there
- `docs/frozen-research-execution-plan.md`
- `docs/presentation-scope.md` deferred high-assurance section
- `docs/milestones.md` M0, M2-A, and frozen-research sections
- `docs/codex-workflow.md`
- `docs/threat-model.md`
- `docs/experiment-protocol.md`
- `docs/architecture.md`
- `docs/experiment-matrix.md`
- `docs/spike-npm12.md`
- `docs/m2-a-npm-lifecycle-adapter.md`
- `docs/reviews/m2-a-npm-lifecycle-adapter.md`
- `docs/m2-a-evidence-transfer-contract.md`
- `docs/reviews/m2-a-evidence-transfer-contract.md`
- `prompts/m2-a-evidence-transfer-implementation.md`
- `prompts/reviews/m2-a-evidence-transfer-implementation-review.md`
- `docs/reviews/m2-a-evidence-transfer-implementation.md`
- `prompts/m2-a-evidence-transfer-implementation-remediation.md`
- every changed production and verification path in the remediation prompt's
  exact allowlist
- this prompt

# Review target

Review only the M2A-TRI01 through M2A-TRI04 remediation diff and its minimal
prompt/status records. Treat reported command passes and finding closure as
claims to reproduce. Do not edit the implementation or verification paths.
Preserve unrelated M3, M4, presentation, and user working-tree changes.

# Required finding decisions

1. **M2A-TRI01:** compare the full plan with the fixed contract. Reproduce
   every absence/volume/create/pre-inspect/start/wait/final-inspect/copy argv,
   exact `/usr/bin/docker`/`shell: false`/deadline/output ceiling, and the exact
   three-key credential-empty environment and empty-directory layout. Reject
   arbitrary or inherited environment, path/layout drift, command/argument
   substitution, fallback, retry, cleanup, and a production execution edge.
2. **M2A-TRI02:** inspect both container sources without importing or
   executing them. Trace every successful open to deterministic close
   settlement, inject or statically reproduce each close-failure family, and
   confirm no ignored close, premature `descriptorsClosed: true`, zero exit,
   or valid attempt can survive unknown initializer or completion-publication
   settlement. Preserve canonical commit, identity, no-repair, and no-late-
   success boundaries.
3. **M2A-TRI03:** reproduce a complete positive completion and table-driven
   invalid terminals for every npm step. Confirm `complete` requires natural
   exit zero, null signal, no timeout, no stdout/stderr truncation, child
   close, and the exact existing approval/lock chain in both the runner and
   `validateCompletionBytes()`.
4. **M2A-TRI04:** enumerate the complete issue vocabulary and code/step
   compatibility matrix. Reproduce incoherent initializer/measurement/
   natural-exit/transfer combinations, arbitrary/path-like issues,
   duplicates/reordering, future or displaced issues, unknown settlement,
   early/extra copies, identity/metadata drift, and retry/cleanup/promotion
   attempts. Confirm only a chronological write-once sanitized candidate
   record is accepted.
5. Confirm the full M2A-TR06 negative matrix is behavioral or structural
   evidence rather than marker-only assertions, the library/backend remain
   pure and fake-only, root scripts remain verification-only, and no path
   outside the exact remediation boundary changed except the saved prompt pair
   and minimal status/review records.
6. Reproduce the exact fixed tuple, 31-file aggregate and anchors, 1/6/0 event
   contract, private inputs, schemas, historical non-reuse, evidence non-
   promotion, and M2A-TR01/M2A-TR02 closure.

# Review method and safety boundary

- Inspect the complete remediation diff, exact allowlist, full arrays/state
  correlations, source ordering, declarations, tests, and static verifier. Do
  not infer closure from names or positive tests alone.
- Run `npm run m2a:transfer:verify`, `npm run m2a:verify`, and root no-emit
  typecheck. Reproduce root tests/check only as proportionate and record any
  unrelated aggregate failure without changing out-of-scope files. Run focused
  formatting and `git diff --check`.
- Use only repository-controlled in-memory inputs and Docker-free source/static
  checks. Do not import either container source or execute any fixture,
  adapter, lifecycle entry, probe, transfer, or runtime wrapper.
- Do not access Docker/runtime sockets, result roots, retained state,
  credentials, host home/environment, external network, Remote Git,
  publication, deployment, or third-party communication. Do not clean up,
  retry, repair, signal, or promote evidence.

# Decision boundary

The maximum positive decision closes M2A-TR01 through M2A-TR06 only at the
Docker-free static/unit implementation boundary. It may permit only one later,
separately defined Docker-free construction/execution-gate contract that must
bind the complete constructed context and exact local `sha256:` image ID before
any candidate command can be reviewed. It does not itself approve construction,
Docker, standing-authorization use, transfer, result access/review, evidence
acceptance, M3 ingestion, profile/matrix/presentation evidence, or `Observed`.
A blocked decision must name only the smallest remaining issue #43 remediation.

# Deliverables

- `docs/reviews/m2-a-evidence-transfer-implementation-remediation.md` with an
  `APPROVED` or `BLOCKED` decision, M2A-TR01 through M2A-TR06 statuses,
  finding-by-finding evidence, exact path/command/environment/settlement/state-
  machine analysis, commands actually run, limitations, and one permitted
  next boundary
- minimal status updates in `docs/m2-a-evidence-transfer-contract.md`,
  `docs/m2-a-npm-lifecycle-adapter.md`, `docs/index.md`,
  `docs/milestones.md`, and `docs/frozen-research-execution-plan.md`
- one concrete `Next:` item

# Completion report

- Decision and M2A-TR01 through M2A-TR06 status
- Independently reproduced command/environment, descriptor settlement,
  complete-child terminal, canonical attempt-state, allowlist, identity, and
  evidence-class boundaries
- Changed files and commands actually run
- Intentionally unrun commands, remaining limitations, and the exact next
  boundary
