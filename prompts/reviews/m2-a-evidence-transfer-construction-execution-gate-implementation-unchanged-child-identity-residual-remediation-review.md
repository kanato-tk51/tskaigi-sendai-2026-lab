# Goal

Perform a fresh independent Docker-free read-only re-review of only frozen-
research issue #43's remaining M2A-CGI04 unchanged-child-identity residual
remediation. Decide whether exact unchanged-child preservation, fixed
operation-specific identity deltas, and matching fake trace coverage close
M2A-CGI04 and M2A-CG04 through M2A-CG06 at the Docker-free static/unit
cooperative-host implementation boundary while preserving closed M2A-CGI01
through M2A-CGI03 and M2A-CG01 through M2A-CG03. Do not repair source or tests,
acquire npm or toolchain bytes, execute a production entry, construct a context
or image, call Docker, run a lifecycle or transfer, or access runtime/result
state.

# Read first

- root `AGENTS.md`, `packages/AGENTS.md`, and `experiments/AGENTS.md`
- `docs/index.md` and the issue #43 documents routed there
- `docs/frozen-research-execution-plan.md`
- `docs/presentation-scope.md`, especially the resumed/deferred high-assurance
  boundary
- `docs/milestones.md` M0, M2-A, and frozen-research sections
- `docs/codex-workflow.md`
- `docs/threat-model.md`
- `docs/experiment-protocol.md`
- `docs/architecture.md`
- `docs/experiment-matrix.md`
- `docs/spike-npm12.md`
- `docs/m2-a-npm-lifecycle-adapter.md`
- `docs/reviews/m2-a-npm-lifecycle-adapter.md`
- `docs/m2-a-evidence-transfer-contract.md`
- every issue #43 transfer implementation/remediation review routed by
  `docs/index.md`
- `docs/m2-a-evidence-transfer-construction-execution-gate.md`
- all construction/execution-gate contract review and remediation records
- all construction/execution-gate implementation prompts, paired review
  prompts, and review records routed by `docs/index.md`
- `prompts/m2-a-evidence-transfer-construction-execution-gate-implementation-private-authority-residual-remediation.md`
- `prompts/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-private-authority-residual-remediation-review.md`
- `docs/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-private-authority-residual-remediation.md`
- `prompts/m2-a-evidence-transfer-construction-execution-gate-implementation-unchanged-child-identity-residual-remediation.md`
- every changed implementation, declaration, verification, test, and status
  path in that prompt's exact allowlists
- this prompt

# Scope

Review only the remaining M2A-CGI04 unchanged-child-identity remediation diff
and its minimal status records. Treat implementation summaries, reported
command passes, source marker names, and positive fake traces as claims to
reproduce. Preserve every unrelated accumulated worktree change and every
immutable earlier review record.

The only permitted repository writes are:

```text
docs/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-unchanged-child-identity-residual-remediation.md
experiments/npm12-install/README.md
docs/m2-a-evidence-transfer-construction-execution-gate.md
docs/m2-a-evidence-transfer-contract.md
docs/m2-a-npm-lifecycle-adapter.md
docs/index.md
docs/milestones.md
docs/frozen-research-execution-plan.md
```

# Out of scope

- Do not edit implementation, declaration, verification, test, prompt,
  package, lockfile, Containerfile, manifest, container source, adapter/probe,
  fixture, scenario, historical/result, Expected, or `Observed` paths.
- Do not reopen M2A-CGI01 through M2A-CGI03, M2A-CG01 through M2A-CG03,
  first-cause process settlement, parent held-directory/path correlation, or
  redesign the reviewed schemas, tuple, aggregates, command order, or evidence
  classes.
- Do not approve or perform acquisition, production construction, image build,
  Docker/runtime, lifecycle, transfer, result validation, cleanup, retry,
  ingestion, publication, deployment, or evidence promotion.

# Constraints

## M2A-CGI04 unchanged-child decision

1. Trace every production inventory, attempt write/rename/replacement, Docker-
   copy destination, `probe-output/` creation, marker-parent mutation, and
   publication call site to one child-identity transition verifier. Confirm
   the separately branded fake trace consumes that same decision and accepts
   no path, descriptor, callback, executable, argv, cwd, environment, process
   handle, production brand, or production constructor.
2. Reproduce the review's controlled contradiction: stable parent inode `P`,
   unchanged `completion.json` child inode `10`, one authorized added
   `segment.json`, and post-operation `completion.json` inode `11`. It must
   reject before after-state adoption or `after:0`, and the next inventory must
   still be bound to inode `10`.
3. Independently change each full identity field of an unchanged same-name
   child: type, device, inode, full mode/special bits, owner/group, link count,
   size, and mtime. Each contradiction must reject despite exact parent
   identity and exact entry names/types. Missing, extra, duplicate, reordered,
   accessor/inherited, sparse, symlink, special, or entry-disconnected child
   maps must also reject before adoption.
4. Reproduce all fixed operation deltas: exclusive `attempt.next` creation;
   exact-identity rename to `attempt.json`, both with and without an existing
   canonical attempt; one fixed top-level copy destination; `probe-output/`
   creation; and one fixed nested marker creation. Confirm every child outside
   the exact delta remains identity-equal and no generic or caller-extensible
   mutation authorization exists.
5. Submit a created destination sharing the device/inode of an unchanged
   sibling, two distinct after names sharing one identity, a renamed
   destination with a different source identity, an unrelated prior attempt or
   copy replacement, and a `probe-output` hardlink/object substitution. Confirm
   all reject before copy, checkpoint, marker, validation, publication, or
   final-result advance.
6. For the nested marker operation, confirm only the marker parent gains
   `direct-write-marker.json`; the transfer parent's existing `probe-output`
   child may change only its operation-caused size/mtime while retaining exact
   type, full mode, owner/group, link count, device, and inode. Confirm all
   transfer siblings remain exact and no full-map refresh hides the nested
   mutation.
7. Confirm validation of the complete before snapshot, exact operation delta,
   and complete after snapshot occurs before updating either held parent or
   child baseline. Search for and reject any path that assigns the entire
   post-operation child map after only parent or entry-name/type validation.
8. Reject marker-only tests, a fake-only reducer not used by production, or
   positives that omit pre-existing siblings. Every controlled negative must
   end its action log at the exact affected `before:<index>` barrier.

## Preserved implementation and evidence boundary

9. Reproduce the exact 31-row and 41-row aggregates and bind the four changed-
   path identities plus this saved prompt pair. Confirm M2A-CGI01 through
   M2A-CGI03, M2A-CG01 through M2A-CG03, M2A-CGR01 through M2A-CGR03, and
   M2A-TR01 through M2A-TR06 remain closed only at their recorded contract or
   Docker-free static/unit scopes.
10. Reproduce the shared first-cause compiler/Docker state-machine positives
    and negatives needed to show the repair did not reopen process settlement.
    Confirm exact parent descriptor/path/full-mode/link correlation, attempt
    write/reread/rename/sync, copied-file metadata validation, canonical
    runtime branches, and fail-closed ownership settlement remain unchanged.
11. Confirm production constructors and brands remain private and absent from
    declarations, exports, package scripts, and tests; fake brands cannot
    substitute for production; reusable imports are side-effect-free; and
    current entry calls still fail before authority creation without importing
    or executing an entry as proof.
12. Confirm future digests/image ID remain `null`, approvals remain `false`,
    every `evidenceReview` remains `not-performed`, and no fixed ignored root,
    acquisition, context, image, Docker object, transfer, result, historical
    evidence, Expected, or `Observed` byte changed.

## Review method and decision ceiling

13. Use only repository source, in-memory calculations, separately branded
    fake process/filesystem traces, and repository-owned disposable test roots.
    Never use a fixed acquisition, construction, build, runtime, or result
    root. Do not import either container source.
14. The maximum positive decision closes M2A-CGI04 and M2A-CG04 through
    M2A-CG06 only at the Docker-free static/unit cooperative-host
    implementation boundary while preserving M2A-CG02/M2A-CG03 closure. It may
    permit only the smallest later separately reviewed acquisition/toolchain
    input boundary already ordered by the contract. It does not acquire bytes,
    approve external communication, construct a context/image, choose an image
    ID, call Docker, approve a runtime occurrence, access a result, ingest M3,
    or promote `Observed`.
15. A blocked decision assigns only the smallest remaining M2A-CGI04 child-
    identity contradiction and keeps every later issue #43 gate frozen. Do not
    repair a finding during this review.

# Deliverables

- `docs/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-unchanged-child-identity-residual-remediation.md`
  with an `APPROVED` or `BLOCKED` decision, exact child-identity and operation-
  delta traces, M2A-CGI01 through M2A-CGI04 and M2A-CG01 through M2A-CG06
  status, changed-path identities, commands run, evidence class, limitations,
  and one permitted next boundary
- Minimal authoritative status updates only after the decision
- One concrete `Next:` item

# Verification

Run only the Docker-free checks needed to reproduce the remediation claims:

```sh
npm run m2a:transfer:verify
npm run m2a:verify
npm run typecheck
npm test
npm run check
git diff --check
```

Also run focused Prettier checking over the exact four-path remediation
allowlist, this saved prompt pair, the new review record, and changed status
files. Record an aggregate failure without editing unrelated files. Never
claim a check passed unless it actually exited successfully.

Do not run `m0:doctor`, `m0:build`, `m0:run`, `m0:verify`, a production entry,
Docker, npm acquisition/install/pack/approve/rebuild, a compiler, a lifecycle,
or any command that touches fixed ignored runtime state.

# Completion report

- Re-review decision and M2A-CGI01 through M2A-CGI04/M2A-CG01 through
  M2A-CG06 status
- Independently reproduced unchanged-sibling, rename/replacement, copy,
  nested-marker, alias, and pre-adoption stop traces
- Preserved process, parent-path, allowlist/import, and evidence-class
  boundaries
- Changed files and commands actually run with observed results
- Intentionally unrun commands, remaining cooperative-host/runtime
  limitations, and preserved unrelated work
- If approved, the smallest separately reviewed acquisition/toolchain input
  boundary without claiming external-network, construction, Docker, runtime,
  or result authority; if blocked, the exact smallest remaining M2A-CGI04
  remediation
- One concrete `Next:` task
