# Goal

Perform a fresh independent Docker-free read-only re-review of only frozen-
research issue #43's residual M2A-CGI02 through M2A-CGI04 implementation
remediation. Decide whether row-local image-build validation, exact runtime
settlement domains, and the complete fixed private production transactions
close M2A-CG01 through M2A-CG06 at implementation scope while preserving the
accepted M2A-CGI01 boundary. Do not repair source or tests, acquire npm or
toolchain bytes, execute a production entry, construct a production context or
image, call Docker, run a lifecycle or transfer, or access runtime/result
state.

# Read first

- root `AGENTS.md`, `packages/AGENTS.md`, and `experiments/AGENTS.md`
- `docs/index.md` and the issue #43 documents routed there
- `docs/frozen-research-execution-plan.md`
- `docs/presentation-scope.md`, especially the resumed/deferred high-assurance
  boundary
- `docs/milestones.md` M0, M2-A, and frozen-research sections
- `docs/codex-workflow.md`
- `docs/threat-model.md`
- `docs/experiment-protocol.md`
- `docs/architecture.md`
- `docs/experiment-matrix.md`
- `docs/spike-npm12.md`
- `docs/m2-a-npm-lifecycle-adapter.md`
- `docs/reviews/m2-a-npm-lifecycle-adapter.md`
- `docs/m2-a-evidence-transfer-contract.md`
- every issue #43 transfer implementation/remediation review routed by
  `docs/index.md`
- `docs/m2-a-evidence-transfer-construction-execution-gate.md`
- all construction/execution-gate contract review and remediation records
- `prompts/m2-a-evidence-transfer-construction-execution-gate-implementation.md`
- `prompts/m2-a-evidence-transfer-construction-execution-gate-implementation-remediation.md`
- `prompts/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-remediation-review.md`
- `docs/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-remediation.md`
- `prompts/m2-a-evidence-transfer-construction-execution-gate-implementation-residual-remediation.md`
- every changed implementation, declaration, verification, and status path in
  the residual-remediation prompt's exact allowlist
- this prompt

# Review target

Review only the residual M2A-CGI02 through M2A-CGI04 remediation diff and its
minimal status records. Treat implementation summaries, reported command
passes, source marker names, and positive fake results as claims to reproduce.
Do not edit implementation, declaration, verification, prompt, package,
fixture, scenario, result, Expected, or `Observed` paths. Preserve every
unrelated accumulated worktree change and immutable earlier review record.

The only permitted repository writes are:

- `docs/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-residual-remediation.md`
  for the fresh re-review decision; and
- minimal status/handoff updates in
  `docs/m2-a-evidence-transfer-construction-execution-gate.md`,
  `docs/m2-a-evidence-transfer-contract.md`,
  `docs/m2-a-npm-lifecycle-adapter.md`, `docs/index.md`,
  `docs/milestones.md`, and `docs/frozen-research-execution-plan.md`.

## Required finding decisions

### Closed M2A-CGI01 preservation

1. Reproduce the fixed 31-row and 41-row aggregates and inspect any shared-
   helper delta. Confirm the exact four-family toolchain union, separately
   derived complete context, canonical schemas, and accepted controlled extra/
   missing/reordered/source-disconnected rejections remain unchanged.
2. Confirm the residual task did not broaden the M2A-CG06 allowlist or reopen
   the npm/toolchain/context input design. Any regression remains blocking even
   though M2A-CGI01 was previously closed.

### M2A-CGI02 — row-local image observation and binding

1. Trace both the fake transaction and fixed production authority one command
   at a time. Each exact terminal and parsed value must be fully validated
   before the next command. No deferred map/batch validation may allow known-
   invalid version, tag-absence, or pinned-base data to reach `offline-build`,
   or invalid build data to reach candidate inspect.
2. Submit the controlled `docker-version` `timedOut: true` input and require the
   action log to be exactly `docker-version`. Independently drift every
   terminal/value field in tag absence and pinned base and confirm the action
   log lacks `offline-build`; drift the build terminal, occurrence, and context
   barrier and confirm it lacks `candidate-inspect`; drift candidate inspect
   and confirm it lacks binding publication.
3. Reproduce the positive complete observation, canonical image-binding bytes,
   `validateImageBindingBytes()` result, exact local `sha256:` binding, and
   settled same-byte publication. Exercise unknown settlement and every known-
   invalid row; confirm neither can publish success, launch a later action,
   retry, clean up, or select an alternate command.

### M2A-CGI03 — exact runtime settlement discrimination

1. Submit `forged-success`, null, numeric, object, missing, extra, inherited,
   accessor, and custom-prototype settlement values at checkpoint publication,
   completion/segment/marker validation, marker-parent preparation, and final
   publication. Only exact own-data `settled` or `unknown` records may be
   classified; no other token may advance or return final success.
2. For validation, require the exact unknown shape
   `null/false/false` and reject any unknown record claiming a digest or closed
   subfield. For publication, require the exact unknown shape
   `false/false/false/false` and reject any unknown record claiming a committed,
   matched, closed, or synced subfield. Exact settled records with a mismatched
   digest or false subfield must take the known-failure path, never success or
   unknown.
3. For every contradiction, inspect the complete action suffix and prove no
   later child, copy, validation, marker-parent creation, final publication,
   retry, or cleanup occurs. Reproduce the positive combined-candidate path and
   confirm the stricter discriminators did not weaken checkpoint order,
   chronological first issue, conditional copy, canonical bytes, or final-
   publication correlation.

### M2A-CGI04 — complete fixed private production authority

1. Statically trace the constructor from its closed entry gate. Before the
   first output, it must no-follow open, register, read, and identity-revalidate
   every tracked/acquisition/toolchain input, including the fixed
   `/usr/bin/node` executable and every receipt-listed `runtime/` closure row.
   Reproduce exact regular-file/mode/size/digest/link/aggregate correlation and
   confirm ordinary `node_modules`, cache, home, environment, caller paths, and
   network are unreachable.
2. Trace both compiler phases. Compiler one must have exact known success and
   all child/output/descriptor settlement before its output is read/copied or
   compiler two launches; compiler two must settle identically before context
   work. Submit known failure and unknown settlement through separately branded
   fake boundaries and confirm no later compiler/read/context/publication
   action.
3. Inspect both compiler and Docker child helpers. Confirm primary deadline,
   250 ms TERM-to-KILL grace, and a 1,000 ms final-close bound resolve explicit
   unknown without requiring an `exit` event. Cover no-exit, exit-without-close,
   synchronous spawn failure, asynchronous error, signal-delivery failure,
   late events, overflow, and descriptor uncertainty; timers and first cause
   must settle exactly once and no case may hang or advance.
4. Trace the production image authority through the same immediate row-local
   barriers reviewed under M2A-CGI02. Static names alone are insufficient:
   verify control flow makes validated version/tag/base strict prerequisites
   of the sole build and validated build a strict prerequisite of inspect and
   binding.
5. Trace the runtime authority's held result/transfer/ancestor descriptors and
   complete phase inventories around every checkpoint, Docker copy,
   same-descriptor copied-file validation, conditional marker-parent operation,
   and final publication. Check exact owner/full-mode/link/BigInt identity,
   ownership registration before first fallible post-open work, no-follow
   access, same-descriptor attempt write/sync/reread/close, rename, directory
   sync, and all-settled release.
6. Exercise controlled replacement, extra/missing entry, symlink/special,
   owner/mode/link/ancestor, attempt-next, copy-destination, marker-parent, and
   pre/post identity drift through separately branded fake filesystem traces.
   Confirm an unknown Docker child leaves only the already synced pessimistic
   checkpoint, performs no later path mutation or Docker action, and settles
   only already owned local handles.
7. Confirm all three production constructors/brands remain private and
   distinct from fakes, absent from declarations/exports/scripts/tests, and
   reachable only after unchanged no-argument gates. Future digests/image ID
   remain `null`, approvals remain `false`, and evidence review remains
   `not-performed`; do not execute an entry to prove it fails before authority
   creation.

### Preserved M2A-CG05/M2A-CG06 and repository boundaries

1. Compare every changed path to the unchanged M2A-CG06 implementation and
   verification allowlist. Confirm package scripts, lockfiles, Containerfile,
   manifest, container sources, adapter/probe sources, package manifests,
   fixtures, scenarios, fixed ignored roots, historical evidence, Expected,
   and `Observed` did not change in the residual remediation.
2. Reproduce descriptor-only exact-own-data rejection, canonical-byte
   handling, private snapshots/brands, import safety, and the complete new
   transaction-level negative matrix. Marker-only source assertions and
   isolated validator passes do not close a transaction or authority finding.
3. Confirm M2A-CGR01 through M2A-CGR03 remain closed at contract scope,
   M2A-CGI01 remains closed at implementation scope, and M2A-TR01 through
   M2A-TR06 remain closed only at their earlier static/unit scope.
   Construction intent, fake observations, candidate bytes, reviewed result,
   M3 ingestion, matrix/profile/presentation evidence, and `Observed` remain
   distinct.

# Review method and safety boundary

- Inspect the complete residual-remediation diff, actual control/data
  correlations, declaration surface, static verifier, and focused tests.
  Independently run controlled in-memory contradictions for all three residual
  findings; do not infer closure from names, prose, markers, or positive tests.
- Use only repository source, in-memory calculations, separately branded fake
  process/filesystem backends, fake timers where needed, and repository-owned
  disposable test roots. Never use a fixed acquisition, toolchain,
  construction, build, runtime, or result root.
- Do not import or execute a production entry or either container source. Do
  not acquire npm/toolchain bytes, compile/construct for production, call
  Docker/runtime sockets, run a lifecycle/probe/transfer, inspect retained or
  result state, clean up, retry, repair, signal a real process, ingest, or
  promote evidence.
- Do not access credentials, host home/environment/cache, external network,
  Remote Git, publication, deployment, or third-party communication. Standing
  authorization is not needed or used for this Docker-free read-only review.

# Decision boundary

The maximum positive decision keeps M2A-CGI01 closed, closes M2A-CGI02 through
M2A-CGI04, and closes M2A-CG01 through M2A-CG06 only at the Docker-free
static/unit implementation boundary. It may permit only the smallest later
separately reviewed prerequisite acquisition/construction gate already ordered
by the contract. It does not acquire bytes, approve external communication,
execute a production entry, construct a context or image, choose a local image
ID, call Docker, approve a runtime occurrence, access a result, accept
evidence, ingest M3, or promote any `Observed` value. A blocked decision must
assign only the smallest remaining M2A-CGI remediation and keep every later
issue #43 gate frozen.

# Deliverables

- `docs/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-residual-remediation.md`
  with an `APPROVED` or `BLOCKED` decision, M2A-CGI01 through M2A-CGI04 and
  M2A-CG01 through M2A-CG06 status, exact contradiction/positive traces,
  changed-path identities, commands run, evidence class, limitations, and one
  permitted next boundary
- Minimal authoritative status updates only after the decision
- One concrete `Next:` item

# Verification

Run only the Docker-free checks needed to reproduce the remediation claims:

```sh
npm run m2a:transfer:verify
npm run m2a:verify
npm run typecheck
npm test
npm run check
git diff --check
```

Also run focused Prettier checking over the exact residual-remediation
allowlist, the saved residual prompt pair, the new review record, and changed
status files. Record an aggregate failure without editing unrelated files.
Never claim a check passed unless it actually exited successfully.

Do not run `m0:doctor`, `m0:build`, `m0:run`, `m0:verify`, a production entry,
Docker, npm acquisition/install/pack/approve/rebuild, or any command that
touches fixed ignored runtime state.

# Completion report

- Re-review decision and M2A-CGI01 through M2A-CGI04/M2A-CG01 through
  M2A-CG06 status
- Independently reproduced row-local image-build, settlement-domain,
  constructor/process, held result/transfer, allowlist/import, and evidence-
  class boundaries
- Changed files and commands actually run with observed results
- Intentionally unrun commands, remaining cooperative-host/runtime
  limitations, and preserved unrelated work
- If approved, the smallest separately reviewed prerequisite acquisition/
  construction gate without claiming network, Docker, runtime, or result
  authority; if blocked, the exact smallest residual M2A-CGI remediation
- One concrete `Next:` task
