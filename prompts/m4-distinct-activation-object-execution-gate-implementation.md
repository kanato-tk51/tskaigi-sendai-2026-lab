# Goal

Implement exactly one bounded Docker-free issue #45 distinct activation-object
execution-gate wrapper/run-ID/package task after the positive M4-AGR01 through
M4-AGR03 remediation re-review. Add the fixed self-contained path-only wrapper,
the two approved `20260720-02` run-ID literals, compiler-produced outputs,
focused static/unit coverage, the one candidate package script, and minimal
handoff records. Do not import or execute the activation object or approve/run
the candidate command.

# Read first

- root `AGENTS.md` and `containers/AGENTS.md`
- `docs/index.md` and `docs/frozen-research-execution-plan.md`
- the issue #45 documents selected by `docs/index.md`
- `docs/m4-distinct-activation-object-execution-gate.md`
- `docs/reviews/m4-distinct-activation-object-execution-gate.md`
- `prompts/m4-distinct-activation-object-execution-gate-remediation.md`
- `docs/reviews/m4-distinct-activation-object-execution-gate-remediation.md`
- this prompt

# Required implementation

1. Replace only the two fixed run-ID literals in `src/constants.ts` with the
   approved permissive/constrained `20260720-02` values and preserve every other
   constant/profile/input/image byte.
2. Add the exact self-contained strict-TypeScript wrapper at the contracted
   source path. Preserve the ordered seven built-in imports, zero repository
   imports, singleton wrapper closure, fixed platform/argument/root checks,
   exact held source/compiled/package identity protocol, two pre-spawn and two
   post-`close` validations, one fixed detached child, empty environment,
   bounded streams/timers/signals, no-second-spawn rule, and complete
   no-child/returned-child/unknown-settlement ownership.
3. Implement strict canonical child/result validation and the chronological
   write-once timeout/output/process/invalid-result latch. Do not emit accepted
   output before stable `close`, both post-use validations, and successful
   reverse descriptor release. Preserve all fixed one/two/four/five step
   prefixes, sanitized identity/spawn terminal branches, root retention, and
   no-retry rules.
4. Add exactly the contracted root script and update only the two profile
   README run-ID handoffs. Construct compiled output only with the pinned
   compile-only command; do not run any wrapper or M4 production command.
5. Extend only the allowlisted static/unit paths to bind exact construction,
   32/64 inventories, 22/22/22/21 activation sets, seven/zero/singleton wrapper
   edges, process/output/identity failure branches, exact roots, no old-root
   access, import safety, and package-only production reachability.
6. Record only static/unit evidence and name the saved fresh independent
   implementation/gate review as next. Do not approve the candidate command.

# Exact change boundary

Use only the implementation/test/document paths allowed by
`docs/m4-distinct-activation-object-execution-gate.md`. Do not change profile
JSON, the activation or ordinary entry, any backend/executor source, fixture,
image/staging input, result, Expected matrix, historical evidence, or
`Observed` value.

# Prohibited actions

- No activation-object or wrapper import/execution.
- No candidate command, M4 doctor/build/recovery/control/evidence command,
  Docker/container/runtime-socket operation, retained-state or result-root
  access, cleanup, retry, repair, signal, or external communication.
- No credentials, host-home/environment inspection, Remote Git, publication,
  deployment, issue #46 work, execution approval, or standing-authorization
  use.

# Verification

Use the pinned compile-only constructor, focused formatting,
`npm run m4:typecheck`, `npm run m4:static`, `npm run m4:test`,
`npm run m4:verify`, the contracted compiled import-safety assertion without
importing either new entry, and `git diff --check`. Report only commands that
actually ran.

# Completion report

Report changed files, commands run, observed static/unit results, commands
intentionally not run, remaining limitations, evidence class, and exactly one
concrete `Next:` task naming the fresh independent implementation/gate review.
