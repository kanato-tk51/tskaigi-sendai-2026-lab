# Goal

Implement exactly one bounded Docker-free frozen-research issue #43
M0/M2-A evidence-transfer static/unit task after the positive M2A-TR01 through
M2A-TR06 contract review. Add only the fixed construction description, pure
validators, fixed initializer/runner source, fake-backend state machine,
static checks, focused tests, verification scripts, and minimal handoff
records allowed by M2A-TR06. Do not build an image, invoke Docker or npm
lifecycle commands, stage runtime input, access a result root or retained
state, or approve/execute the one-shot occurrence.

# Read first

- root `AGENTS.md`, `packages/AGENTS.md`, and `experiments/AGENTS.md`
- `docs/index.md` and the issue #43 documents routed there
- `docs/frozen-research-execution-plan.md`
- `docs/m2-a-evidence-transfer-contract.md`
- `docs/reviews/m2-a-evidence-transfer-contract.md`
- `prompts/reviews/m2-a-evidence-transfer-contract-review.md`
- this prompt

# Required implementation

1. Preserve the fixed `20260721-01` tuple, all historical M0/M2-A bytes, the
   ordered 31-file source aggregate, and the exact M2-A route/attempt contract.
2. Add a Docker-free, non-executable construction description and pure fixed
   Docker command/inspection/canonical-record validators. Accept no arbitrary
   command, argument, environment, mount, path, image tag, container, volume,
   result root, or runtime option.
3. Add the no-argument fixed initializer and measurement-runner sources with
   exact private-input, npm-flow, loopback, settlement, completion publication,
   canonical-byte, and no-late-operation boundaries. Do not import or execute
   either source during verification.
4. Add a fake-backend-only host state machine covering absence preflight,
   volume/initializer/measurement lifecycle, natural-exit-only fixed copies,
   chronological write-once failure, unknown-settlement stop, retention, and
   candidate-only classification. Do not add a production Docker backend or
   executable production entry.
5. Add focused negative/static coverage for every M2A-TR06 category, including
   tuple/hash/order drift, arbitrary input, policy/mount/copy drift, settlement,
   canonical bytes, filesystem identity metadata, completion/segment/marker
   consistency, fallback/retry/cleanup/promotion, and broad reachability.
6. Record only Docker-free static/unit evidence and name the fresh independent
   implementation review as the next task. Do not bind a local image ID or
   approve construction/execution.

# Exact change boundary

Change only paths listed in the `M2A-TR06` allowlist in
`docs/m2-a-evidence-transfer-contract.md`. Do not change any adapter or
`probe-core` source, fixture manifest, historical/raw/sanitized result,
profile, scenario, Expected value, or Observed value.

# Prohibited actions

- No Docker/container/runtime-socket operation, image build, npm
  install/pack/approve/rebuild, lifecycle/probe execution, transfer, staging,
  result-root or retained-state access, cleanup, retry, repair, or signaling.
- No credentials, host-home/environment inspection, external network, Remote
  Git, publication, deployment, implementation approval, execution approval,
  or standing-authorization use.
- Do not import or execute either new container source as a verification
  shortcut.

# Verification

Run only the new focused Docker-free M2-A transfer verification, the existing
M2-A static/unit verification needed for regression, root typecheck/test as
proportionate, focused formatting, and `git diff --check`. Report only commands
actually run and keep aggregate failures distinct from the focused task result.

# Completion report

Report changed files, commands run, observed static/unit results, commands
intentionally not run, remaining limitations, evidence class, and exactly one
concrete `Next:` task naming the fresh independent Docker-free implementation
review.
