# Goal

Perform one bounded Docker-free remediation of M4-AGI01 and M4-AGI02 from the
fresh issue #45 distinct activation-object implementation/gate review. Make the
independently reviewed complete wrapper bytes and edge contract an exact
pre-execution input, and complete the contract-required focused negative
matrix. Do not run or approve the candidate command.

# Read first

- root `AGENTS.md` and `containers/AGENTS.md`
- `docs/index.md` and `docs/frozen-research-execution-plan.md`
- the active issue #45 documents selected by `docs/index.md`
- `docs/m4-distinct-activation-object-execution-gate.md`
- `docs/reviews/m4-distinct-activation-object-execution-gate.md`
- `prompts/m4-distinct-activation-object-execution-gate-remediation.md`
- `docs/reviews/m4-distinct-activation-object-execution-gate-remediation.md`
- `prompts/m4-distinct-activation-object-execution-gate-implementation.md`
- `docs/reviews/m4-distinct-activation-object-execution-gate-implementation.md`
- this prompt

# Required remediation

1. Close M4-AGI01 without claiming that a current-byte baseline alone
   authenticates independently reviewed bytes. Fix the contract and
   implementation boundary so the exact 42,865 / 41,159 / 1,244-byte wrapper
   source/JavaScript/declaration and their reviewed SHA-256 values are an
   explicit pre-execution trust input. Preserve complete held descriptors and
   twice-pre/twice-post validation after that input is established. If exact
   full-byte authenticity must remain an external fresh-worker preflight
   because the wrapper cannot non-circularly authenticate its own bytes, state
   that split explicitly, bind the exact preflight values and adjacency to the
   one command occurrence, and retain the cooperative-host race limitation.
   Do not silently weaken the reviewed-byte requirement or treat prefix checks
   as full-byte authentication.
2. Bind the exact seven source/emitted built-in edges, zero repository edges,
   singleton wrapper closures, package-only production inbound edge, parent
   inventories, and full wrapper hashes at the same trust boundary. Reject a
   same-prefix pre-capture body change and any extra loading form before command
   eligibility.
3. Add deterministic focused negatives for every missing, extra, reordered,
   alternate built-in binding; absolute, relative, `file:`, package/subpath,
   dynamic/computed, `require`, `createRequire`, property-based/evaluation,
   activation/ordinary/backend/runner, emitted-binding, and otherwise unleased
   edge required by the contract.
4. Complete the process/output acceptance matrix for separate third/fourth
   no-child validation failures, reverse-release suppression, null streams,
   invalid PID, missing/duplicate/contradictory exit-close tuples, asynchronous
   error with and without later `close`, TERM/KILL delivery failure, exact
   root-access instrumentation, and both valid complete and valid
   Inconclusive bytes after each earlier timeout/output/process/signal failure.
   Bind exact one/two/four/five prefixes, outer key order, private-field
   suppression, post-use checks, and reverse release.
5. Preserve the fixed `20260720-02` generation, constants/profile/activation
   bytes, one-child process object, chronological write-once latch, root
   retention, no-retry rule, ordinary fail-closed entry, historical evidence,
   and `Observed` boundaries. Save a fresh independent Docker-free remediation
   re-review prompt and make it the only next task.

# Exact change boundary

Use only the existing execution-gate contract/status records, wrapper
source/compiler outputs if required by the exact trust model, wrapper focused
test, static verifier, import-safety/static-safety tests only if directly
needed, this remediation/re-review prompt pair, and minimal issue #45 handoff
records. Do not change canonical profile JSON, activation-entry source,
ordinary entry, backend/executor implementation, fixture, image/staging input,
run IDs, result, Expected matrix, historical evidence, or `Observed`.

# Prohibited actions

- No wrapper or activation production entry import/execution and no candidate
  command.
- No Docker/container/runtime-socket action, result-root or retained-state
  access, cleanup, repair, retry, signaling, external network, credentials,
  host-home inspection, Remote Git, publication, deployment, or external
  communication.
- Do not use standing authorization; no runtime command is approved by this
  remediation.

# Verification

Use only repository-owned Docker-free compiler/AST/schema assertions, bounded
disposable unit fixtures, the contract-approved compile/static/unit commands,
the compiled ordinary-module import assertion without either frozen-research
entry, focused formatting, and `git diff --check`. Do not probe a result root or
run either entry.

# Completion report

Report the exact M4-AGI01/M4-AGI02 changes, trust-anchor decision, new negative
matrix, commands actually run, commands intentionally not run, evidence
classes, residual limitations, and exactly one `Next:` item naming the fresh
independent remediation re-review. Do not approve the candidate command or
claim a separate human review.
