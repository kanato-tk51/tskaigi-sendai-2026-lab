# Goal

Define one Docker-free, review-ready contract for a fresh selected-Vite
measurement generation that adds a fixed container init/reaping prerequisite
without treating the fifth Inconclusive attempt's unresolved process-group
residue as a demonstrated zombie or reaping cause. Do not implement or execute
the contract.

# Read first

- root `AGENTS.md`
- `containers/AGENTS.md`
- `packages/AGENTS.md`
- `docs/index.md`
- `docs/presentation-scope.md`
- `docs/p2-vite-completion.md`
- `docs/p2-vite-detached-transfer-contract.md`
- `docs/reviews/p2-vite-detached-transfer-result.md`
- `docs/milestones.md` selected-Vite addenda and current issue #54 handoff
- `docs/codex-workflow.md`
- `docs/threat-model.md`
- this prompt

# Scope

- Classify the fifth attempt's durable facts, tracked static/unit boundaries,
  supported inference, and unresolved runtime facts separately.
- Reserve exactly one new immutable selected-Vite identity tuple.
- Define the exact fixed Docker `--init` create-plan addition and the created
  and final inspect binding needed to prove only that the option was configured.
- Preserve the current fail-closed child-process-group, progress-transfer,
  evidence-access, receipt, permissive-first, same-image, and no-retry rules.
- Define focused Docker-free implementation acceptance criteria and the ordered
  later review, implementation, staging, execution-gate, one-shot, and result-
  review boundaries.
- Save a fresh independent contract-review prompt before any implementation.
- Update only the minimal selected-Vite and frozen-research handoff records.

# Out of scope

- Production code, schema, test, staging, scenario, Expected, or result changes
- Docker/container commands, runtime sockets, probes, lifecycle fixtures, build,
  verification, staging, execution, or result-root access
- Selecting or accepting a receipt, changing selected-Vite or experiment-matrix
  `Observed`, or changing accepted codegen/P3/P4 evidence
- Retry, repair, cleanup, reinterpretation, or inspection of any of the five
  immutable historical attempts
- Frozen M4 implementation, Remote Git, publication, deployment, or external
  communication

# Constraints

- Follow the root and nested `AGENTS.md` files.
- Keep every new identity caller-independent and reject every prior Vite
  identity in any later implementation.
- Do not claim that Docker `--init` resolves the historical failure. It is a
  configuration hypothesis until a separately gated runtime attempt exists.
- Do not weaken post-close-residue handling: detected residue remains child
  failure even when later bounded force and reaping establish group absence.
- Do not add an arbitrary init path, executable, argument, image, mount,
  environment variable, network, or caller input.
- Do not use external network, credentials, ignored runtime state, or a
  container-runtime socket.

# Deliverables

- `docs/p2-vite-init-reaper-contract.md`
- `prompts/reviews/p2-vite-init-reaper-contract-review.md`
- Minimal authoritative selected-Vite and frozen-research handoff updates
- No production or runtime-state change

# Verification

Use only fixed-path tracked-source assertions needed to support the contract's
static claims, a focused local Prettier check over changed Markdown files, and
`git diff --check`. Do not run Docker, a probe/lifecycle fixture, build,
staging, execution, result inspection, or broad verification merely to repeat
prior reports.

# Completion report

- Changed files
- Commands run and observed results
- Observed, static, inferred, and unresolved evidence classes
- Exact proposed contract and implementation acceptance boundary
- Commands intentionally not run and remaining limitations
- `Next:` fresh independent Docker-free contract review
