# Experiments

Experiments are intentionally outside the root npm workspace. The M0 marker-only npm 12 spike lives under `npm12-install/`; its lifecycle is packaged and executed only inside its disposable container. M1 and later experiments are not implemented.
