# M0/M2-A construction/execution-gate remediation re-review

## Review target and decision

- Target: frozen-research issue #43's bounded M2A-CGR01 through M2A-CGR03
  construction/execution-gate contract remediation
- Review type: fresh independent Docker-free read-only contract re-review
- Review prompt:
  [m2-a-evidence-transfer-construction-execution-gate-remediation-review](../../prompts/reviews/m2-a-evidence-transfer-construction-execution-gate-remediation-review.md)
- Decision: **BLOCKED; no static/unit implementation, acquisition,
  construction, image build, or execution task is approved**
- Finding status: M2A-CGR01 and M2A-CGR02 close; M2A-CGR03 remains open on
  the single exact checkpoint-identity gap below
- Contract repair or implementation performed in this review: none

The remediation closes the separately reviewed constructor-toolchain and
canonical construction schema, plus the private offline image-build and
canonical image-binding packet. It also resolves the former unknown-outcome
publication contradiction with a durable prelaunch checkpoint. The existing
attempt vocabulary, however, cannot identify which of the three distinct
absence-preflight Docker children is the exact next child represented by that
checkpoint. Standing authorization cannot choose new persistent step
identities and was not used.

## Independently reproduced identities and preserved boundaries

The review reproduced the exact existing 31-row input inventory and aggregate:

```text
sha256:4fcdb65f903e257fc6eb0ac3d12f028312ed5e8c14d09c9a68f4cc3b536aef04
```

Appending the contract's ten rows in their exact order reproduced 41 rows and
the unchanged aggregate:

```text
sha256:fe3da65eaf3a727892599bc8c5ecc30fedded5ef0b44614c5629762539f56526
```

All ten tuple fields matched between the tracked manifest and pure transfer
library. The current Containerfile retained exactly eight `COPY` sources, each
covered by one construction context family. The lockfile also reproduced the
contract's exact TypeScript `5.9.3`, `@types/node` `20.19.43`, and
`undici-types` `6.21.0` versions and integrity strings, including
`@types/node`'s exact `~6.21.0` dependency on `undici-types`.

The separate npm and constructor-toolchain receipts remain unperformed future
prerequisites. This review did not inspect either private root or infer a
receipt digest, inventory aggregate, runtime closure, or package bytes from
the current host installation.

## M2A-CGR decision summary

| Item | Re-review decision |
| --- | --- |
| M2A-CGR01 — compiler authority and construction schema | **CLOSED at contract scope** |
| M2A-CGR02 — exact offline build and image-binding packet | **CLOSED at contract scope** |
| M2A-CGR03 — exact host transaction and unknown-settlement rule | **BLOCKED on exact absence-checkpoint identity** |

## M2A-CG decision summary

| Item | Contract-scope decision |
| --- | --- |
| M2A-CG01 — immutable source and acquisition closure | **CLOSED at contract scope** |
| M2A-CG02 — complete deterministic construction manifest | **CLOSED at contract scope** |
| M2A-CG03 — offline one-build image identity and retention | **CLOSED at contract scope** |
| M2A-CG04 — exact production entry and one-shot lifecycle | **BLOCKED by M2A-CGR03** |
| M2A-CG05 — failure, result, and evidence separation | **BLOCKED by M2A-CGR03's inexact durable unknown record** |
| M2A-CG06 — later implementation and verification allowlist | **BLOCKED by M2A-CGR03** |

## Finding-by-finding re-review

### M2A-CGR01 — closed at contract scope

The remediated `m2a-transfer-toolchain/v1` receipt is a separate
not-yet-authorized prerequisite at one fixed private repository path. Its
ordered runtime, package, regular-file inventory, aggregate, status, and review
fields bind the fixed `/usr/bin/node` executable/runtime closure and the exact
three package trees without permitting root `node_modules`, cache, home,
caller, alternate executable, or network fallback. The receipt remains
`evidenceReview: "not-performed"` until a separate independent review binds
its canonical bytes and inventory.

The constructor and its two compiler processes are fixed to the private
workspace, exact executable and argv order, empty environment, deadlines,
output bound, TERM/KILL/close settlement, copied probe-core dependency, and
complete pre-manifest cleanup/descriptor settlement. The full ordered
`m2a-transfer-construction/v1` top-level/nested key sequences, receipt/source
bindings, two compiler rows, lexical context inventory, nullability, epoch
mtime, canonical bytes, and aggregate input are fixed. No production
compiler, constructor, toolchain, or context was run or observed here.

### M2A-CGR02 — closed at contract scope

The remediated build contract fixes the private credential-empty `HOME` and
`DOCKER_CONFIG` layout, exact config bytes and modes, `/usr/bin/docker`,
repository-root cwd, three-key environment, five literal argv arrays, command
order, deadlines, output/termination/close bounds, and exact Docker `29.6.1`,
base, tag-absence, one-build, platform, and candidate projections. The plan has
no runtime container, volume, transfer, result, registry fallback, retry, or
cleanup command.

The complete ordered `m2a-transfer-image-binding/v1` schema binds the reviewed
construction and receipt identities, pinned base projection, sole build
occurrence and policy, one future exact local `sha256:` candidate ID, complete
platform/config projection, retention, `runtimeExecutionApproved: false`, and
`evidenceReview: "not-performed"`. Publication follows full command/output/
descriptor/context settlement. No Docker command or image inspection was run
in this review, so no actual local image ID exists as review evidence.

### M2A-CGR03 — one exact checkpoint identity remains open

The remediated host filesystem transaction otherwise fixes the result-root cwd
for relative copies, owner/type/link/mode/ancestor checks, completion-first
conditional destinations, exclusive `.next` write/sync/reread/close/rename/
directory-sync publication, post-child settlement, and the consistent unknown
rule. An unknown child close permits no subsequent Docker, inspect, copy,
validation, cleanup, or filesystem publication; the already committed
prelaunch bytes remain the durable sanitized Inconclusive record.

The fixed Docker plan nevertheless starts with three distinct children:

```text
["volume","inspect","tskaigi-m2a-evidence-20260721-01"]
["container","inspect","tskaigi-m2a-transfer-init-20260721-01"]
["container","inspect","tskaigi-m2a-transfer-run-20260721-01"]
```

The current canonical `m2a-transfer-attempt/v1` vocabulary represents all
three only as:

```json
{"code":"M2A_SETTLEMENT_UNKNOWN","step":"absence-preflight"}
```

The in-memory model validated that one canonical checkpoint and reproduced
three plan children but only one distinct persisted step identity. No attempt
field binds the volume, initializer-container, or measurement-container
absence argv. Therefore the bytes committed before a child cannot prove which
exact next Docker child was launched if its close becomes unknown. This does
not reopen the write-ahead publication rule; it leaves only its exact child
correlation incomplete.

## Exact smallest remediation

One further prompt-first Docker-free contract remediation must define three
distinct fixed canonical preflight checkpoint identities and their one-to-one
mapping to the existing volume, initializer-container, and measurement-
container absence argv. It must bind those identities into the attempt
vocabulary/validation boundary used by the later implementation while
preserving the already reviewed checkpoint transaction, command order,
write-once first issue, and no-post-unknown rule. It must not implement that
boundary, change any Docker argv, acquire bytes, construct an image, or access
runtime/result state in the prompt-only task.

## Evidence classes and remaining limitations

- This is contract-review evidence. Hash and source-plan reproduction does not
  establish compiler, filesystem, Docker, image, lifecycle, named-volume,
  transfer, or result behavior.
- The npm distribution and constructor toolchain receipts remain absent or
  otherwise unreviewed by this task; acquisition is not authorized.
- No constructor, production entry, context, image binding, exact local image
  ID, runtime attempt, accepted result, matrix/profile/presentation evidence,
  or `Observed` value exists from this review.
- Historical M0 remains Inconclusive, and the named-volume copy path remains
  unobserved.

## Verification observed

| Command | Observed result |
| --- | --- |
| `git status --short --branch` and scoped repository inspection | Existing multi-session dirty worktree inventoried and preserved; no cleanup or reset. |
| Repository-controlled read-only Node identity/context/plan/checkpoint audit | Reproduced 31 and 41 rows with both exact aggregates, ten tuple fields, eight Containerfile copy sources, three exact lockfile packages/integrities, three absence Docker children, and one accepted canonical absence-preflight checkpoint step. |
| Focused Prettier check over the remediated contract, prompt pair, new review, and five status records | Exit `0`; all matched files use Prettier style. |
| `git diff --check` | Exit `0`; no whitespace error reported in the accumulated tracked diff. |

No test is required for this documentation-only review. No compiler build, npm
pack/install/approve/rebuild, constructor, lifecycle entry, adapter, probe,
Docker/container/runtime-socket command, transfer, ignored acquisition/
construction/build/result-root access, retained-state access, external
network, Remote Git, publication, deployment, third-party communication, or
standing authorization was used.

Next: save the exact bounded Docker-free M2A-CGR03 absence-checkpoint identity
remediation prompt and fresh independent re-review prompt; do not acquire npm
or toolchain bytes, construct a context or image, call Docker, or access
runtime/result state.
