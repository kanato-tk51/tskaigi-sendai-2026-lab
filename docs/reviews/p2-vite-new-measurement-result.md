# P2 selected Vite `20260720-01` result independent review

## Review target and decision

- Target: the exhausted `20260720-01` one-shot Vite outcome, its exact
  canonical permissive v3 attempt, the approved seventeen-file source identity,
  the pure bounded pair/entry projection, and only the four fixed result paths
  named by the result-review contract
- Review type: fresh independent Docker-free read-only result review
- Decision: **ACCEPTED only as the fourth immutable Inconclusive execution
  attempt**
- Blocking findings: none
- Non-blocking findings: none
- Retry or runtime recovery: prohibited and not performed
- Selected Vite and experiment-matrix Observed: unchanged and unmeasured

The exact canonical record and approved pure projectors agree. The permissive
attempt remains Inconclusive, the pair remains Inconclusive with only the
permissive scenario represented, and no receipt, constrained attempt,
same-image pair, or capability outcome exists. The bounded progress prefix is
accepted as diagnostic secondary state only.

This review changed no Vite executor, runner, adapter, probe, staging, result,
Expected, or experiment-matrix byte. Review-owned changes are this record, the
smallest deterministic tracked presentation projection/generator update, and
minimal authoritative status/handoff metadata. This is an independent Codex
review, not a separate human review.

## Canonical attempt and fixed root states

Exact checks of only the approved paths reproduced:

- the permissive root is a non-symlink directory with mode `0700`;
- `attempt.json` is a non-symlink regular file with mode `0600`, exactly 823
  bytes, exactly one canonical JSON line, and SHA-256
  `9175487c2ed92eb8265e9047c362bc1d0a42d79e1911ba951fcf235530f6eada`;
- the exact permissive `summary.json` is absent; and
- the exact constrained root is absent.

The canonical object has exactly the reviewed seventeen top-level fields. It
binds schema `p2-vite-attempt/v3`, scenario/profile/run
`vite-observe-p / permissive / p2-vite-observe-p-20260720-01`, Expected
`p2-vite-expected-20260720-01`, and the approved image ID
`sha256:4ada13d4258db3809cbff56d605f80af8383bf1f823168d0518d8dce799e7cf0`.
It records:

- `inconclusive` with primary
  `attached-start / P2_EXECUTOR_DOCKER_TIMEOUT`;
- null container exit, known Docker CLI settlement, runner settlement
  `not-established`, completed cleanup, and null runner disposition;
- output `not-inspected`; and
- only `P2_ATTEMPT_DOCKER_LIFECYCLE_FAILED` and
  `P2_ATTEMPT_OUTPUT_NOT_INSPECTED`.

The nested progress object has exactly `schemaVersion`, `validity`, and
`stages`. It is `p2-vite-progress/v1`, `valid-prefix`, with the exact ordered
prefix `runner-entered`, `inputs-prepared`, `service-ready`, `child-launched`.
Canonical-byte checks found no raw stdout/stderr/error, path, command,
environment, canary, timestamp, duration, container-name, or result-root field.

## Approved source identity

All seventeen execution-gate SHA-256 values were independently reproduced.
They establish source-byte identity, not runtime enforcement or the lower-level
timeout cause.

| Target | SHA-256 |
|---|---|
| `containers/presentation-profiles/src/plan.ts` | `a51227f045c77f3b3f23fa310413d57078c689a2d53a5f5d3336ff3374ae87aa` |
| `containers/presentation-profiles/src/vite-projection.ts` | `ced7b9572f862d77283a44ac453b570c469b5a085635c7330bd768df9cf26ca2` |
| `containers/presentation-profiles/src/vite-executor.ts` | `18cef6133fdb82f7ab8cb176d435d107a8db2cc6d24a3c449d4e9eb6cba81818` |
| `containers/presentation-profiles/runner/vite-executor-entry.js` | `daeee923a7887fc7747c39058a5d59a4b02bdcca55a3692653b2980d16d02dc9` |
| `containers/presentation-profiles/runner/vite-executor-entry.d.ts` | `1246122cc5cbda3b9a50872e3f25451aecfb0e9df4b2103718564b4d86532e10` |
| `containers/presentation-profiles/runner/vite-runner.js` | `e590a23b1a81da97cccde3017bdf8f707bb00821135d48fae2fdb13f29b2a934` |
| `containers/presentation-profiles/runner/vite-runner.d.ts` | `00680f692ef3f31f1cf1b9e6b57e29c6d6041acfb67188fd2ba232f98fbfbf20` |
| `containers/presentation-profiles/runner/vite-staging.js` | `d23c64bf12b6df49dd7a1666acf410d4777b9c758f8f0b0550413808bdda7cd5` |
| `containers/presentation-profiles/runner/vite-staging.d.ts` | `2fa86743232202602ad7d5a60745d1e12b68050c9d8e084b091fbe70ebd316d4` |
| `containers/presentation-profiles/test/plan.test.ts` | `05a1b59477184a1cfbaea1bd5fd68bd57b586231cae3062870b2fe9e1d38ca06` |
| `containers/presentation-profiles/test/vite-projection.test.ts` | `cb9718d99e696cdd42cecbc21889e441d31be030e257e34a3afecff3bb331ccb` |
| `containers/presentation-profiles/test/vite-runner.test.ts` | `98a398dbaff65bbd7e2929b99c4f5b98376e06305ab8c3d56ec3a6426bbb8112` |
| `containers/presentation-profiles/test/vite-executor.test.ts` | `bd331ac0a71e0464805771d0dea472093c62d731139d5096c07440f814917e7e` |
| `containers/presentation-profiles/test/vite-staging.test.ts` | `b7a4a5bde14dfb093da8882f37667f099fe9f0c7b469695f9f5059be9c42d0d0` |
| `packages/vite-plugin-probe/src/scenario-context.ts` | `cab4afe7954cb6e10473831b70a0c148ce15be0596157ebf2aa35131f3a6654c` |
| `packages/vite-plugin-probe/test/scenario-context.test.ts` | `272cf2ce33a9ada16ea393a200569cd720055f4b67e9e9f79cd7532602d588b9` |
| Root `package.json` | `aa01a921db70b7333cf45d24fd44c43ca1e187501b75af416b13b29f77c17f98` |

## Pure bounded projection reconstruction

After the approved Docker-free build, the compiled pure projectors reproduced:

- pair schema `p2-vite-pair/v3`, Expected
  `p2-vite-expected-20260720-01`, validity `inconclusive`, null image ID, and
  only `PAIR_IDENTITY_MISMATCH` because no complete receipt pair exists;
- entry status `inconclusive`; and
- one permissive scenario with completion `inconclusive`, a written attempt,
  evidence `not-inspected`, receipt `not-written`, validity `not-inspected`,
  and the two attempt issues above.

The projectors receive no receipt derived from the progress prefix. Therefore
the prefix establishes neither runner/child settlement nor evidence access,
cannot create `summary.json`, cannot manufacture a constrained outcome, and
cannot promote a capability observation or same-image pair.

## Evidence classification and unresolved facts

- **Observed Inconclusive attempt facts:** the accepted fixed-path bytes and
  modes, exact v3 fields, primary host-side attached-start timeout, known Docker
  CLI settlement, completed cleanup, valid four-stage prefix, absent summary,
  and absent constrained root.
- **Supported bounded interpretation:** under the reviewed static protocol, the
  accepted prefix means the fixed runner reached a valid child launch before
  the host-side attached-start command timed out.
- **Unresolved runtime facts:** child settlement, service settlement, output
  export, container exit, the lower-level timeout cause, how much time belonged
  to Docker attach transport, and every capability outcome.

The three `20260719-*` attempts remain immutable and separate. This result does
not reinterpret any of them or establish a repeated common lower-level cause.

## Tracked presentation projection

The tracked `profiles.json` and deterministic evidence-map generator now retain
all four immutable Inconclusive attempts side by side. The fourth entry records
the exact v3 hash, primary diagnostic, and bounded progress prefix. Every Vite
permissive capability remains `not-inspected`, every constrained capability
remains `missing`, and the talk still has exactly three tables. Accepted
codegen, P3, and P4 evidence classes are unchanged; no Expected value,
experiment-matrix cell, or selected Vite Observed status was changed.

## Safety boundary and remaining limitations

This review did not call Docker, retry the pair, access a runtime socket, read
an event/direct-write/tool-output or historical evidence subtree, enumerate a
parent result directory, inspect a historical container/result, change a
retained permission, repair/move/delete runtime state, pull an image, use
external network or credentials, access frozen M4 state, perform Remote Git,
publish, deploy, or communicate with a third party. The
`continue-repository-work` standing authorization was not needed by this
Docker-free review.

No Vite receipt, constrained attempt, same-image pair, capability outcome, or
selected-profile Observed evidence exists. The one-shot gate is exhausted and
no retry or runtime recovery is authorized.

## Verification observed

| Command or assertion | Observed result |
|---|---|
| Approved source SHA-256 calculation | Exit 0; all seventeen execution-gate identities reproduced exactly. |
| Exact canonical attempt/root assertion | Exit 0; canonical one-line v3 fields, non-symlink `0700`/`0600` modes, 823 bytes, SHA-256 `9175487c...`, absent summary, and absent constrained root reproduced. |
| `npm run p2:build` | Exit 0; probe-core, M2-D, M2-E, and presentation TypeScript outputs compiled without Docker execution. |
| Pure bounded pair/entry reconstruction | Exit 0; the approved projectors reproduced the exact Inconclusive pair and one-scenario entry projection without a result write. |
| `npm run p2:verify` | Exit 0; P2 typecheck and 9 test files / 152 tests passed. |
| `npm run p4:generate` and `npm run p4:verify` | Exit 0; the evidence map regenerated exactly and 1 focused file / 2 tests passed with four Vite attempts in exactly three talk tables. |
| `npm run check` | Exit 0; formatting, lint, typecheck, and 103 test files / 743 tests passed. |
| Protected-path tracked diff assertion | Exit 0; accepted codegen/P3/M4 bytes plus non-Vite route/artifact projections were unchanged. |
| `npm run format:check` and `git diff --check` | Exit 0 after the review-owned projection and handoff updates. |
| `git status --short` | Captured the existing uncommitted `20260720-01` candidate/history plus this review and projection update; no unrelated change was reverted. |

Next: none.
