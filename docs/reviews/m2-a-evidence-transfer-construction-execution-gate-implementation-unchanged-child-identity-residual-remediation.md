# M0/M2-A unchanged-child-identity residual-remediation re-review

## Review target and decision

- Target: frozen-research issue #43's bounded M2A-CGI04 unchanged-child-
  identity residual-remediation candidate
- Review type: fresh independent Docker-free read-only re-review
- Review prompt:
  [m2-a-evidence-transfer-construction-execution-gate-implementation-unchanged-child-identity-residual-remediation-review](../../prompts/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-unchanged-child-identity-residual-remediation-review.md)
- Decision: **APPROVED at the Docker-free static/unit cooperative-host
  implementation boundary**
- Closed in this review: the remaining M2A-CGI04 unchanged-child-identity
  finding and implementation-scope M2A-CG04 through M2A-CG06
- Preserved closed: M2A-CGI01 through M2A-CGI03 and M2A-CG01 through M2A-CG03
- Source or test repair performed in this review: none

The production backend and the separately branded fake trace now call the same
`assertProductionDirectoryTransition()` and child-identity decision. Every
snapshot binds one lexical entry to one complete child record, and each fixed
operation permits only its named addition, rename/replacement, or correlated
nested mutation. Every other child's type, full mode, effective owner/group,
link count, device, inode, size, and mtime must remain exact before either held
baseline is updated.

The original process first-cause and held-parent/path findings remain closed.
The complete bounded implementation now closes M2A-CGI04 and M2A-CG04 through
M2A-CG06 only at static/unit scope. It does not acquire dependency or toolchain
bytes, construct or publish a production context, build or inspect an image,
call Docker, approve a runtime occurrence, transfer or review a result, or
promote evidence.

Standing authorization was not needed or used. This review did not execute a
production entry, access a fixed ignored root or retained state, import either
container source, spawn a compiler or Docker process, run npm install/pack/
approve/rebuild, run a lifecycle or transfer, use external communication, or
change historical, Expected, or `Observed` evidence.

## Reproduced identities and allowlist

The review opened the 31-row source and 41-row construction inventories
read-only with `O_NOFOLLOW`, required stable one-link regular-file BigInt
identity before and after each read, and settled all 72 descriptor closes. The
reproduced aggregates are:

```text
31 rows: sha256:4fcdb65f903e257fc6eb0ac3d12f028312ed5e8c14d09c9a68f4cc3b536aef04
41 rows: sha256:fe3da65eaf3a727892599bc8c5ecc30fedded5ef0b44614c5629762539f56526
```

The exact four-path remediation allowlist and saved prompt-pair identities are:

| Path | SHA-256 |
| --- | --- |
| `experiments/npm12-install/scripts/m2a-transfer-production.mjs` | `2643baca57d1ef77c9b3c763e3d73724274d960bc8b261cf11d37a7d387c2a39` |
| `experiments/npm12-install/scripts/m2a-transfer-production.d.mts` | `f5e588d497f85eaa527516fee3d6b3e28b4480a55ced6b68f61330bb0cb46815` |
| `experiments/npm12-install/scripts/verify-m2a-transfer-static.mjs` | `856124f5a1f63e0c7c459ccfc3588ac4abe468027e06981ed90e0001a09619c7` |
| `tests/m2a-evidence-transfer.test.ts` | `796346a06e880ef84d33a1c02dd585339b69811e5a55067a1ad1c23fff36ed55` |
| `prompts/m2-a-evidence-transfer-construction-execution-gate-implementation-unchanged-child-identity-residual-remediation.md` | `7ade81263d4d19a809b5cddecbfb4024edd102f80ed247de9200ef107a7be2a0` |
| `prompts/reviews/m2-a-evidence-transfer-construction-execution-gate-implementation-unchanged-child-identity-residual-remediation-review.md` | `3a17df45f9e85229e97a43699baff78a422b78fc26597dc2c9f3c5e839bf5187` |

No implementation path outside the prompt's strict four-path subset changed
for this remediation. Production authority constructors and brands remain
private and absent from declarations, package scripts, and tests. The fake
declarations expose only canonical plain child snapshots and the six fixed
operation kinds; they carry no path, descriptor, callback, executable, argv,
cwd, environment, process handle, production brand, or production constructor.

Future acquisition, toolchain, construction-manifest, context, and image
digests remain `null`; build and runtime approvals remain `false`; every
`evidenceReview` remains `not-performed`. The no-argument entries still reject
before private authority creation under those constants; an entry was not
imported or called to demonstrate that static boundary.

## Child-identity and fixed-operation decision

`validateAbstractDirectorySnapshot()` rejects Proxy, accessor, inherited,
custom-prototype, sparse, missing, extra, duplicate, reordered, special,
symlink-shaped, type-disconnected, and cross-name device/inode-alias data. It
requires sorted one-to-one entry and child inventories with full type/mode,
owner/group, link, device/inode, size, and mtime values. Production inventory
capture constructs that same snapshot only after held-descriptor/path identity
correlation before and after enumeration.

`assertDirectoryChildTransition()` has no caller-extensible mutation list. Its
six exact operation kinds bind:

1. exclusive `attempt.next` creation;
2. exact-inode rename to `attempt.json`, with or without replacing the prior
   canonical attempt;
3. fixed completion copy creation;
4. fixed producer-segment copy creation;
5. fixed `probe-output/` directory creation; and
6. fixed nested `direct-write-marker.json` creation together with only the
   inode-stable `probe-output` size/mtime update in the transfer parent.

An independent in-memory matrix reproduced seven positive operation
occurrences: the two-step initial attempt publication, prior-attempt
replacement, both top-level copies, `probe-output/` creation, and the
correlated nested marker. Every pre-existing sibling remained complete-
identity equal. The nested case validated both parents before assigning either
new parent or child baseline.

The prior review's controlled contradiction was reproduced with stable parent
inode `P`, `transfer-completion.json` child inode `10`, one authorized fixed
producer-segment addition, and post-operation completion inode `11`. It
rejected with the action log exactly `before:0`; there was no `after:0`, and the
only accepted baseline remained the before snapshot bound to inode `10`.

The same matrix changed, one at a time, the unchanged completion child's type,
device, inode, permission mode, special bit, owner, group, link count, size,
and mtime. All ten cases rejected at `before:0`. Ten malformed snapshot cases
cover missing, extra, reordered, inherited, accessor, sparse, disconnected,
symlink-shaped, special, and cross-name alias data. Wrong rename identity,
unrelated sibling replacement, created-destination hardlink alias, and
`probe-output` object substitution also stopped at `before:0`.

A two-step pre-adoption trace accepted only the first exact `attempt.next`
creation, then supplied a replaced unchanged completion child as the next
before snapshot. Its action log ended at
`before:0, after:0, before:1`, proving the accepted inode-10 map had not been
replaced by the contradictory second input. Both fake trace brands rejected
plain-object forgeries.

## Preserved process, parent-path, and evidence boundaries

Both production child helpers still consume the same private first-cause/
first-exit process state machine as the separately branded fake process trace.
The independent preservation matrix reproduced twelve settled unsuccessful
branches, including spawn/error/exit/close contradictions, timeout, overflow,
signal failure, absent exit or close, descriptor uncertainty, and duplicate
exit. One exact zero/null exit and identical close succeeded and ignored one
post-settlement error. Both fake-brand forgeries rejected.

Production still holds repository, result-ancestor, result, transfer, and
conditional marker-parent descriptors and correlates exact BigInt descriptor
and no-follow path identity. Attempt write/reread/close, rename, held-directory
sync, copied-file metadata validation, canonical runtime branches, and
fail-closed ownership settlement remain in place. Unknown copy settlement
marks every affected parent uncertain and suppresses transition completion.

These checks are cooperative-host pre/post correlation, not an OS filesystem
sandbox. Another same-authority actor can still race pathname components or
mount state between observations. Static/unit success does not establish npm
or toolchain acquisition, production compilation/publication, Docker behavior,
local image identity, lifecycle behavior, named-volume transfer, result
validity, runtime enforcement, or `Observed` evidence.

## Decision status

| Item | Re-review decision |
| --- | --- |
| M2A-CGI01 — exact input closure | **CLOSED; preserved** |
| M2A-CGI02 — row-local image observation/binding | **CLOSED; preserved** |
| M2A-CGI03 — exact runtime settlement branches | **CLOSED; preserved** |
| M2A-CGI04 — complete fixed private authority | **CLOSED at Docker-free static/unit cooperative-host scope** |

| Item | Implementation-scope decision |
| --- | --- |
| M2A-CG01 — immutable source/acquisition closure | **CLOSED; preserved** |
| M2A-CG02 — deterministic construction manifest | **CLOSED; preserved at Docker-free static/unit scope** |
| M2A-CG03 — offline one-build image identity | **CLOSED; preserved at Docker-free static/unit scope** |
| M2A-CG04 — production entry/one-shot lifecycle | **CLOSED at Docker-free static/unit scope; not runtime-approved** |
| M2A-CG05 — failure/result/evidence separation | **CLOSED at Docker-free static/unit scope; no result accepted** |
| M2A-CG06 — allowlist/negative coverage/import safety | **CLOSED at Docker-free static/unit scope** |

M2A-CGR01 through M2A-CGR03 remain closed at contract scope. M2A-TR01 through
M2A-TR06 remain closed only at their earlier Docker-free static/unit transfer
scope. No acquisition, construction, image-build, runtime, transfer, result,
or evidence gate is opened by this decision.

## Verification observed

| Command | Observed result |
| --- | --- |
| `git status --short --branch` and scoped source inspection | Existing accumulated dirty worktree inventoried and preserved; no cleanup or reset. |
| First independent child/process diagnostic | Exit `1`; the reviewer used conceptual `completion.json`/`segment.json` names instead of the contract constants, so fixed completion-copy validation rejected. No file changed; the corrected matrix below replaced this invalid diagnostic. |
| Corrected repository-controlled child/process matrix | Exit `0`; seven fixed operation occurrences, 26 child/snapshot/alias/pre-adoption negatives, twelve process negatives, one process positive, and both fake-brand rejections behaved as described. |
| Descriptor-controlled aggregate calculation | Exit `0`; exact 31/41 aggregates reproduced and all 72 descriptor closes settled. |
| `sha256sum` over the four implementation paths and saved prompt pair | Exit `0`; recorded the six current identities above. |
| `npm run m2a:transfer:verify` | Exit `0`; static verifier passed and 1 file / 48 tests passed. |
| `npm run m2a:verify` | Exit `0`; adapter typecheck/build/static passed and 4 files / 5 tests passed. |
| `npm run typecheck` | Exit `0`; root no-emit typecheck passed. |
| `npm test` | Exit `0`; 109 files / 851 tests passed. |
| `npm run check` | Exit `1`; aggregate checking stopped at the pre-existing out-of-scope formatting warning for `containers/profile-control/test/control-host-backend.test.ts` before lint/typecheck/test. |
| Focused Prettier check over the exact four-path remediation allowlist, prompt pair, this review, and seven status paths | Exit `0`; all matched files use Prettier style. |
| `git diff --check` | Exit `0`; no whitespace error was reported in the accumulated tracked diff. |

The review intentionally did not run `m0:doctor`, `m0:build`, `m0:run`,
`m0:verify`, a production entry, Docker, npm acquisition/install/pack/approve/
rebuild, a production compiler, a lifecycle, a transfer, result validation,
cleanup, retry, or evidence promotion.

## Next boundary

The smallest later safe task is prompt-first and Docker-free: define the exact
separately reviewed npm-acquisition/constructor-toolchain input boundary that
the construction contract already orders. It may save only the contract and
fresh review prompts; it must not acquire or inspect the future bytes, use
external communication, execute a production entry, construct a context,
build an image, call Docker, access runtime/result state, or promote evidence.

Next: save the exact Docker-free issue #43 npm-acquisition/constructor-
toolchain input-boundary contract and fresh independent review prompt; do not
acquire or inspect those future bytes or perform construction, Docker,
transfer, or runtime/result work.
